local RS        = game:GetService("ReplicatedStorage")
local plrs      = game:GetService("Players")
local http      = game:GetService("HttpService")
local localPlr  = plrs.LocalPlayer

local pkgs  = RS:WaitForChild("Packages")
local idx   = pkgs:WaitForChild("_Index")
local net   = idx:WaitForChild("leifstout_networker@0.3.1"):WaitForChild("networker")
local rmt   = net:WaitForChild("_remotes")

local dataCli = require(pkgs.DataService).client
dataCli:waitForData()

local rollRmt = rmt:WaitForChild("RollService"):WaitForChild("RemoteFunction")
local codeRmt = rmt:WaitForChild("CodeService"):WaitForChild("RemoteFunction")
local invRmt  = rmt:WaitForChild("InventoryService"):WaitForChild("RemoteFunction")
local rebRmt  = rmt:WaitForChild("RebirthService"):WaitForChild("RemoteFunction")
local zoneRmt = rmt:WaitForChild("ZonesService"):WaitForChild("RemoteFunction")
local upgRmt  = rmt:WaitForChild("UpgradeService"):WaitForChild("RemoteFunction")
local boostRmt= rmt:WaitForChild("BoostService"):WaitForChild("RemoteFunction")
local offRmt  = rmt:WaitForChild("OfflineEarningsService"):WaitForChild("RemoteFunction")
local idxRmt  = rmt:WaitForChild("IndexService"):WaitForChild("RemoteFunction")

local src = RS:WaitForChild("Source", 30)
if not src then warn("Main module missing") return end

local itms     = src:WaitForChild("Game"):WaitForChild("Items")
local rollFeat = src:WaitForChild("Features"):WaitForChild("Roll")
local mutFolder= src:WaitForChild("Features"):WaitForChild("Mutations")

local rollSlice   = require(rollFeat:WaitForChild("RollSlice"))
local slimeData   = require(itms:WaitForChild("Slimes"))
local rarityTiers = require(itms:WaitForChild("RarityTiers"))
local mutations   = require(mutFolder:WaitForChild("Mutations"))
local upgTree     = require(RS.Source.Features.Upgrades.UpgradeTree)
local idxRewards  = require(RS.Source.Features.Index.IndexRewards)

-- // HELPERS

local function formatNumber(n)
    if type(n) ~= "number" then return tostring(n) end
    local a = math.abs(n)
    local s, d = "", 1
    if     a >= 1e18 then s = "Qn"; d = 1e18
    elseif a >= 1e15 then s = "Qd"; d = 1e15
    elseif a >= 1e12 then s = "T";  d = 1e12
    elseif a >= 1e9  then s = "B";  d = 1e9
    elseif a >= 1e6  then s = "M";  d = 1e6
    elseif a >= 1e3  then s = "K";  d = 1e3
    end
    if s ~= "" then
        local fm = n / d
        if math.abs(fm - math.floor(fm)) < 0.01 then
            return string.format("%d%s", math.floor(fm), s)
        else
            return string.format("%.1f%s", fm, s):gsub("%.0f", "")
        end
    end
    return tostring(math.floor(n))
end

local function getRarityFromOdds(odds)
    if not odds or odds <= 0 then return "Unknown" end
    local ok, tier = pcall(rarityTiers.getTier, odds)
    return (ok and tier and tier.name) or "Unknown"
end

local function getFirstItem(res)
    if type(res) ~= "table" then return nil end
    for _, e in ipairs(res) do
        if type(e) == "table" and e.id then return e end
    end
    return nil
end

local function getRollFingerprint(res)
    if type(res) ~= "table" then return "nil" end
    local cnt = #res
    if cnt == 0 then return "empty" end
    local parts = {}
    for i, r in ipairs(res) do
        local it = getFirstItem(r)
        parts[i] = it and tostring(it.id) or tostring(i)
    end
    return cnt.."|"..table.concat(parts, ",")
end

local function ordinal(n)
    local s = tostring(n)
    local last, tens = n % 10, n % 100
    if tens >= 11 and tens <= 13 then return s.."th" end
    if last == 1 then return s.."st" end
    if last == 2 then return s.."nd" end
    if last == 3 then return s.."rd" end
    return s.."th"
end

local function getThumbnailUrl(assetId)
    if not assetId then return nil end
    local url = "https://thumbnails.roblox.com/v1/assets?assetIds="..assetId.."&size=420x420&format=Png&isCircular=false"
    local ok, res = pcall(request, {Url = url, Method = "GET"})
    if ok and res and res.Success then
        local data = http:JSONDecode(res.Body)
        if data and data.data and data.data[1] then
            return data.data[1].imageUrl
        end
    end
    return nil
end

local function getMutationSize(muts)
    if not muts then return 64 end
    if muts.huge then return 128 elseif muts.big then return 96 end
    return 64
end

local function getMutationColor(muts)
    if not muts then return nil end
    if muts.inverted then return 0x9b59b6
    elseif muts.huge then return 0xf1c40f
    elseif muts.big  then return 0xe67e22
    elseif muts.shiny then return 0xf39c12
    end
    return nil
end

local function parseLuck(val)
    if type(val) == "number" then return val end
    if type(val) ~= "string" or val == "" then return 0 end
    local mult = 1
    local v = val
    local suffix = string.upper(string.sub(v, -1))
    if suffix == "B" then mult = 1e9; v = string.sub(v, 1, -2)
    elseif suffix == "M" then mult = 1e6; v = string.sub(v, 1, -2)
    elseif suffix == "K" then mult = 1e3; v = string.sub(v, 1, -2)
    end
    local n = tonumber(v)
    return n and (n * mult) or 0
end

-- // RAYFIELD UI

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name             = "Game Hub",
    Icon             = 0,
    LoadingTitle     = "Loading Interface",
    LoadingSubtitle  = "Please wait...",
    Theme            = "Default",
    ToggleUIKeybind  = "K",
    DisableRayfieldPrompts  = false,
    DisableBuildWarnings    = false,
    ConfigurationSaving = {
        Enabled    = true,
        FolderName = nil,
        FileName   = "GameHub"
    },
    Discord = {
        Enabled      = false,
        Invite       = "",
        RememberJoins = true
    },
    KeySystem = false,
})

-- // MAIN TAB

local MainTab = Window:CreateTab("Main", 138602335586757)

MainTab:CreateSection("Welcome")

MainTab:CreateParagraph({
    Title   = "Enabled By Default",
    Content = "[+] Anti AFK"
})

MainTab:CreateParagraph({
    Title   = "Latest Update",
    Content = "[+] Added Auto Feed with team split support\n[+] Added Auto Complete Index with rarity filtering\n[+] Added Webhook ping & rarity filters\n\n[i] Report bugs at discord.gg/idiothub"
})

MainTab:CreateButton({
    Name     = "Join Our Discord",
    Callback = function() end,
})

MainTab:CreateParagraph({
    Title   = "Configuration",
    Content = "Your configuration is saved automatically on change. You may also save manually below."
})

MainTab:CreateButton({
    Name     = "Save Config Manually",
    Callback = function()
        Rayfield:SaveConfiguration()
    end,
})

-- // FARMING TAB

local FarmingTab = Window:CreateTab("Farming", 138602335586757)

FarmingTab:CreateSection("Zones")

local StayInBestZoneToggle = FarmingTab:CreateToggle({
    Name         = "Auto Stay In Best Unlocked Zone",
    CurrentValue = false,
    Flag         = "FarmingStayInBestZone",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while Rayfield.Flags.FarmingStayInBestZone.CurrentValue do
                    local MAX = 25
                    for z = MAX, 1, -1 do
                        if not Rayfield.Flags.FarmingStayInBestZone.CurrentValue then break end
                        local ok = pcall(zoneRmt.InvokeServer, zoneRmt, "requestTeleportZone", z)
                        if ok then
                            task.wait(1)
                            local nz = dataCli:get("zone") or 1
                            if nz == z then break end
                        end
                    end
                    task.wait(10)
                end
            end)
        end
    end,
})

local UnlockAffordableZonesToggle = FarmingTab:CreateToggle({
    Name         = "Auto Unlock Affordable Zones",
    CurrentValue = false,
    Flag         = "FarmingUnlockAffordableZones",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while Rayfield.Flags.FarmingUnlockAffordableZones.CurrentValue do
                    pcall(zoneRmt.InvokeServer, zoneRmt, "requestPurchaseZone")
                    task.wait(5)
                end
            end)
        end
    end,
})

FarmingTab:CreateSection("Slimes")

local EquipBestSlimesToggle = FarmingTab:CreateToggle({
    Name         = "Auto Equip Best Slimes",
    CurrentValue = false,
    Flag         = "FarmingEquipBestSlimes",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                local d = 30
                while Rayfield.Flags.FarmingEquipBestSlimes.CurrentValue do
                    pcall(invRmt.InvokeServer, invRmt, "requestEquipBest")
                    task.wait(d)
                    d = math.min(d * 2, 600)
                end
            end)
        end
    end,
})

local TransferLevelsToggle = FarmingTab:CreateToggle({
    Name         = "Auto Transfer Levels To Best Slime",
    CurrentValue = false,
    Flag         = "FarmingTransferLevels",
    Callback     = function(Value)
    end,
})

local AutoFeedToggle = FarmingTab:CreateToggle({
    Name         = "Auto Feed",
    CurrentValue = false,
    Flag         = "FarmingAutoFeed",
    Callback     = function(Value)
    end,
})

local AutoFeedModeDropdown = FarmingTab:CreateDropdown({
    Name           = "Feed Mode",
    Options        = {"Best Slime", "Whole Team Split"},
    CurrentOption  = {"Best Slime"},
    MultipleOptions = false,
    Flag           = "FarmingAutoFeedMode",
    Callback       = function(Options) end,
})

FarmingTab:CreateSection("Rolling")

local FastRollToggle = FarmingTab:CreateToggle({
    Name         = "Auto Fast Roll",
    CurrentValue = false,
    Flag         = "FarmingFastRoll",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while Rayfield.Flags.FarmingFastRoll.CurrentValue do
                    pcall(rollRmt.InvokeServer, rollRmt, "requestRoll")
                    task.wait()
                end
            end)
        end
    end,
})

FarmingTab:CreateSection("Loot")

local CollectLootToggle = FarmingTab:CreateToggle({
    Name         = "Auto Collect Loot",
    CurrentValue = false,
    Flag         = "FarmingCollectLoot",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                local lootRmt = RS:WaitForChild("Packages")
                    :WaitForChild("_Index")
                    :WaitForChild("leifstout_networker@0.3.1")
                    :WaitForChild("networker")
                    :WaitForChild("_remotes")
                    :WaitForChild("LootService")
                    :WaitForChild("RemoteFunction")
                while Rayfield.Flags.FarmingCollectLoot.CurrentValue do
                    task.wait(30)
                    local lootFolder = workspace:FindFirstChild("Loot")
                    if lootFolder then
                        for _, l in ipairs(lootFolder:GetChildren()) do
                            pcall(lootRmt.InvokeServer, lootRmt, "requestCollect", l.Name)
                        end
                    end
                end
            end)
        end
    end,
})

-- // GAME TAB

local GameTab = Window:CreateTab("Game", 82493603309814)

GameTab:CreateSection("Rebirth")

local AutoRebirthToggle = GameTab:CreateToggle({
    Name         = "Auto Rebirth",
    CurrentValue = false,
    Flag         = "GameAutoRebirth",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while Rayfield.Flags.GameAutoRebirth.CurrentValue do
                    local rbs  = dataCli:get("rebirths") or 0
                    local gp   = dataCli:get("goop") or 0
                    local fz   = dataCli:get("furthestZone") or 0
                    local cost = (2 ^ rbs) * 500
                    local minZone = tonumber(Rayfield.Flags.GameMinZoneRebirth.CurrentValue) or 0
                    if fz >= minZone and gp >= cost then
                        pcall(rebRmt.InvokeServer, rebRmt, "requestRebirth")
                    end
                    task.wait(10)
                end
            end)
        end
    end,
})

local MinZoneRebirthInput = GameTab:CreateInput({
    Name                   = "Minimum Zone To Rebirth",
    CurrentValue           = "",
    PlaceholderText        = "e.g. 10",
    RemoveTextAfterFocusLost = false,
    Flag                   = "GameMinZoneRebirth",
    Callback               = function(Text) end,
})

GameTab:CreateSection("Upgrades")

local AutoUpgradeToggle = GameTab:CreateToggle({
    Name         = "Auto Upgrade Purchasing",
    CurrentValue = false,
    Flag         = "GameAutoUpgrade",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                local function collectUpgrades(t, ids)
                    if type(t) ~= "table" then return end
                    for id, d in pairs(t) do
                        if type(d) == "table" and d.cost then
                            table.insert(ids, id)
                        end
                    end
                end
                local function getCost(id)
                    local function search(t)
                        if type(t) ~= "table" then return nil end
                        for k, d in pairs(t) do
                            if k == id and type(d) == "table" then return d.cost end
                        end
                    end
                    return search(upgTree.main)
                        or search(upgTree.main.lootTree and upgTree.main.lootTree.subTree)
                        or search(upgTree.main.playerTree and upgTree.main.playerTree.subTree)
                end
                local upgs = {}
                collectUpgrades(upgTree.main, upgs)
                if upgTree.main.lootTree and upgTree.main.lootTree.subTree then
                    collectUpgrades(upgTree.main.lootTree.subTree, upgs)
                end
                if upgTree.main.playerTree and upgTree.main.playerTree.subTree then
                    collectUpgrades(upgTree.main.playerTree.subTree, upgs)
                end
                while task.wait(0.5) and Rayfield.Flags.GameAutoUpgrade.CurrentValue do
                    local mode   = Rayfield.Flags.GameUpgradeMode.CurrentOption[1] or "All"
                    local owned  = dataCli:get("upgrades") or {}
                    local coins  = dataCli:get("coins") or 0
                    local goop   = dataCli:get("goop") or 0
                    local rc     = dataCli:get("rollCurrency") or 0
                    for _, id in ipairs(upgs) do
                        if not owned[id] then
                            local cost = getCost(id)
                            if cost then
                                local amt = cost.amount or 0
                                local cur = cost.currency
                                local modeOk = mode == "All"
                                    or (mode == "Coins" and cur == "coins")
                                    or (mode == "Goop"  and cur == "goop")
                                    or (mode == "Rolls" and cur == "rollCurrency")
                                local affordable = (cur == "coins" and coins >= amt)
                                    or (cur == "goop" and goop >= amt)
                                    or (cur == "rollCurrency" and rc >= amt)
                                if modeOk and affordable then
                                    local ok = pcall(upgRmt.InvokeServer, upgRmt, "requestUnlock", id)
                                    if ok then task.wait(0.2) end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end,
})

local UpgradeModeDropdown = GameTab:CreateDropdown({
    Name            = "Upgrade Mode",
    Options         = {"All", "Goop", "Coins", "Rolls"},
    CurrentOption   = {"All"},
    MultipleOptions = false,
    Flag            = "GameUpgradeMode",
    Callback        = function(Options) end,
})

GameTab:CreateSection("Enemies")

local TeleportEnemiesToggle = GameTab:CreateToggle({
    Name         = "Teleport To Nearby Enemies",
    CurrentValue = false,
    Flag         = "GameTeleportEnemies",
    Callback     = function(Value) end,
})

local SuperFastEnemiesToggle = GameTab:CreateToggle({
    Name         = "Super Fast Enemies",
    CurrentValue = false,
    Flag         = "GameSuperFastEnemies",
    Callback     = function(Value) end,
})

local EnemySpeedSlider = GameTab:CreateSlider({
    Name         = "Enemy Speed",
    Range        = {0, 200},
    Increment    = 1,
    Suffix       = "",
    CurrentValue = 16,
    Flag         = "GameEnemySpeed",
    Callback     = function(Value) end,
})

GameTab:CreateSection("Slime Speed")

local SuperFastSlimeToggle = GameTab:CreateToggle({
    Name         = "Super Fast Slime Speed",
    CurrentValue = false,
    Flag         = "GameSuperFastSlime",
    Callback     = function(Value) end,
})

local SlimeSpeedSlider = GameTab:CreateSlider({
    Name         = "Slime Speed",
    Range        = {0, 500},
    Increment    = 1,
    Suffix       = "",
    CurrentValue = 16,
    Flag         = "GameSlimeSpeed",
    Callback     = function(Value) end,
})

GameTab:CreateSection("Index")

local AutoCompleteIndexToggle = GameTab:CreateToggle({
    Name         = "Auto Complete Index",
    CurrentValue = false,
    Flag         = "GameAutoCompleteIndex",
    Callback     = function(Value) end,
})

local IndexModeDropdown = GameTab:CreateDropdown({
    Name            = "Index Mode",
    Options         = {"Normal", "Shiny", "Big", "Huge", "Inverted"},
    CurrentOption   = {"Normal"},
    MultipleOptions = true,
    Flag            = "GameIndexMode",
    Callback        = function(Options) end,
})

-- // MISC TAB

local MiscTab = Window:CreateTab("Misc", 96334002390551)

MiscTab:CreateSection("Codes & Rewards")

local RedeemCodesToggle = MiscTab:CreateToggle({
    Name         = "Auto Redeem Codes",
    CurrentValue = false,
    Flag         = "MiscRedeemCodes",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                local codes = dataCli._data._data._codes or {}
                local codeList = {"SLIMETIME", "RELEASE", "UPDATE1", "FREEGOOP"}
                for _, cd in ipairs(codeList) do
                    if not codes[cd] then
                        pcall(codeRmt.InvokeServer, codeRmt, "redeem", cd)
                        task.wait(0.5)
                    end
                end
            end)
        end
    end,
})

local ClaimOfflineToggle = MiscTab:CreateToggle({
    Name         = "Auto Claim Offline Earnings",
    CurrentValue = false,
    Flag         = "MiscClaimOffline",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while Rayfield.Flags.MiscClaimOffline.CurrentValue do
                    pcall(offRmt.InvokeServer, offRmt, "requestClaim")
                    task.wait(60)
                end
            end)
        end
    end,
})

local ClaimIndexToggle = MiscTab:CreateToggle({
    Name         = "Auto Claim Index Rewards",
    CurrentValue = false,
    Flag         = "MiscClaimIndex",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                local function claimRewards()
                    local d = dataCli:get("index")
                    if not d or not d.categories then return end
                    for cat, rw in pairs(idxRewards) do
                        local cd = d.categories[cat]
                        if cd then
                            local unlocked = cd.unlocked or {}
                            local cnt = 0
                            for _, v in pairs(unlocked) do
                                if v == true then cnt = cnt + 1 end
                            end
                            local claimed = cd.claimedRewards or {}
                            for _, r in ipairs(rw) do
                                if cnt >= r.req and not claimed[r.key] then
                                    pcall(idxRmt.InvokeServer, idxRmt, "requestClaimReward", cat)
                                    task.wait(0.5)
                                end
                            end
                        end
                    end
                end
                while Rayfield.Flags.MiscClaimIndex.CurrentValue do
                    claimRewards()
                    task.wait(60)
                end
            end)
        end
    end,
})

local ClaimIndexModeDropdown = MiscTab:CreateDropdown({
    Name            = "Index Reward Mode",
    Options         = {"All", "Normal", "Shiny", "Big", "Huge", "Inverted"},
    CurrentOption   = {"All"},
    MultipleOptions = false,
    Flag            = "MiscClaimIndexMode",
    Callback        = function(Options) end,
})

MiscTab:CreateSection("Consumables")

local UsePotionsToggle = MiscTab:CreateToggle({
    Name         = "Auto Use Potions",
    CurrentValue = false,
    Flag         = "MiscUsePotions",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while task.wait(1) and Rayfield.Flags.MiscUsePotions.CurrentValue do
                    local boosts = dataCli:get("boosts") or {}
                    local selected = Rayfield.Flags.MiscPotionTypes.CurrentOption or {}
                    for _, p in ipairs(selected) do
                        local d = boosts[p]
                        if d and (d.amount or 0) > 0 then
                            pcall(boostRmt.InvokeServer, boostRmt, "requestUseBoost", p)
                        end
                    end
                end
            end)
        end
    end,
})

local PotionTypesDropdown = MiscTab:CreateDropdown({
    Name            = "Potion Types",
    Options         = {"Luck", "Speed", "Coins", "Goop", "XP"},
    CurrentOption   = {"Luck"},
    MultipleOptions = true,
    Flag            = "MiscPotionTypes",
    Callback        = function(Options) end,
})

local UseDiceToggle = MiscTab:CreateToggle({
    Name         = "Auto Use Dice & Items",
    CurrentValue = false,
    Flag         = "MiscUseDice",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while task.wait(1) and Rayfield.Flags.MiscUseDice.CurrentValue do
                    local items = dataCli:get("items") or {}
                    local selected = Rayfield.Flags.MiscDiceTypes.CurrentOption or {}
                    for _, d in ipairs(selected) do
                        if (items[d] or 0) > 0 then
                            pcall(invRmt.InvokeServer, invRmt, "requestUseItem", d)
                        end
                    end
                end
            end)
        end
    end,
})

local DiceTypesDropdown = MiscTab:CreateDropdown({
    Name            = "Dice & Item Types",
    Options         = {"Luck Dice", "Golden Dice", "Mystery Box", "Chest"},
    CurrentOption   = {"Luck Dice"},
    MultipleOptions = true,
    Flag            = "MiscDiceTypes",
    Callback        = function(Options) end,
})

MiscTab:CreateSection("Social")

local FriendRequestsToggle = MiscTab:CreateToggle({
    Name         = "Auto Send & Accept Friend Requests",
    CurrentValue = false,
    Flag         = "MiscFriendRequests",
    Callback     = function(Value)
        if Value then
            task.spawn(function()
                while Rayfield.Flags.MiscFriendRequests.CurrentValue do
                    pcall(function()
                        local friendService = game:GetService("FriendService")
                        for _, player in ipairs(plrs:GetPlayers()) do
                            if player ~= localPlr then
                                local rel = localPlr:GetFriendStatus(player.UserId)
                                if rel == Enum.FriendStatus.NotFriend then
                                    localPlr:RequestFriendship(player)
                                elseif rel == Enum.FriendStatus.FriendRequestReceived then
                                    localPlr:AcceptFriendRequestAsync(player.UserId)
                                end
                            end
                        end
                    end)
                    task.wait(300)
                end
            end)
        end
    end,
})

-- // WEBHOOK TAB

local WebhookTab = Window:CreateTab("Webhook", 84577758013974)

WebhookTab:CreateSection("Configuration")

WebhookTab:CreateButton({
    Name     = "Test Webhook",
    Callback = function()
        local url = Rayfield.Flags.WebhookURL.CurrentValue
        if not url or url == "" then
            Rayfield:Notify({
                Title   = "Webhook",
                Content = "Please enter a Webhook URL first.",
                Duration = 4,
            })
            return
        end
        local uid = Rayfield.Flags.WebhookUserID.CurrentValue
        local ping = (uid and uid ~= "") and ("<@"..uid.."> ") or ""
        local payload = {
            content  = ping,
            username = "Game Hub",
            embeds   = {{
                title       = "âœ… Webhook Test",
                description = "Your webhook is working correctly!",
                color       = 0x2ecc71,
            }}
        }
        local ok, err = pcall(function()
            request({
                Url     = url,
                Method  = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body    = http:JSONEncode(payload)
            })
        end)
        Rayfield:Notify({
            Title   = "Webhook",
            Content = ok and "Test sent successfully!" or "Failed to send: "..(err or "unknown error"),
            Duration = 4,
        })
    end,
})

local WebhookURLInput = WebhookTab:CreateInput({
    Name                     = "Webhook URL",
    CurrentValue             = "",
    PlaceholderText          = "https://discord.com/api/webhooks/...",
    RemoveTextAfterFocusLost = false,
    Flag                     = "WebhookURL",
    Callback                 = function(Text) end,
})

local UserIDInput = WebhookTab:CreateInput({
    Name                     = "User ID To Ping",
    CurrentValue             = "",
    PlaceholderText          = "Enter Discord User ID",
    RemoveTextAfterFocusLost = false,
    Flag                     = "WebhookUserID",
    Callback                 = function(Text) end,
})

WebhookTab:CreateSection("Filters")

local SendInvertedToggle = WebhookTab:CreateToggle({
    Name         = "Send Inverted",
    CurrentValue = false,
    Flag         = "WebhookSendInverted",
    Callback     = function(Value) end,
})

local SendMutationsToggle = WebhookTab:CreateToggle({
    Name         = "Send Mutations",
    CurrentValue = false,
    Flag         = "WebhookSendMutations",
    Callback     = function(Value) end,
})

local RarityDropdown = WebhookTab:CreateDropdown({
    Name            = "Rarity To Send",
    Options         = {"Normal", "Rare", "Epic", "Legendary", "Mythical", "Secret"},
    CurrentOption   = {"Legendary"},
    MultipleOptions = true,
    Flag            = "WebhookRarity",
    Callback        = function(Options) end,
})

local MinimumLuckInput = WebhookTab:CreateInput({
    Name                     = "Minimum Luck To Send",
    CurrentValue             = "",
    PlaceholderText          = "e.g. 1000000",
    RemoveTextAfterFocusLost = false,
    Flag                     = "WebhookMinLuck",
    Callback                 = function(Text) end,
})

-- // SETTINGS TAB

local SettingsTab = Window:CreateTab("Settings", 122930981612451)

SettingsTab:CreateSection("System")

local AntiAFKToggle = SettingsTab:CreateToggle({
    Name         = "Anti AFK",
    CurrentValue = true,
    Flag         = "SettingsAntiAFK",
    Callback     = function(Value)
        if Value then
            local VirtualUser = game:GetService("VirtualUser")
            game:GetService("Players").LocalPlayer.Idled:Connect(function()
                if Rayfield.Flags.SettingsAntiAFK.CurrentValue then
                    VirtualUser:CaptureController()
                    VirtualUser:ClickButton2(Vector2.new())
                end
            end)
        end
    end,
})

local AntiKickToggle = SettingsTab:CreateToggle({
    Name         = "Anti Kick",
    CurrentValue = false,
    Flag         = "SettingsAntiKick",
    Callback     = function(Value) end,
})

local AutoRejoinToggle = SettingsTab:CreateToggle({
    Name         = "Auto Rejoin On Disconnect",
    CurrentValue = false,
    Flag         = "SettingsAutoRejoin",
    Callback     = function(Value) end,
})

SettingsTab:CreateSection("Performance")

local OptimizationToggle = SettingsTab:CreateToggle({
    Name         = "Optimization",
    CurrentValue = false,
    Flag         = "SettingsOptimization",
    Callback     = function(Value)
        if Value then
            pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/malikstt/script/main/Optimization.lua"))()
            end)
        end
    end,
})

local GPUSaverToggle = SettingsTab:CreateToggle({
    Name         = "GPU Saver",
    CurrentValue = false,
    Flag         = "SettingsGPUSaver",
    Callback     = function(Value) end,
})

SettingsTab:CreateSection("Display")

local DisplayFPSToggle = SettingsTab:CreateToggle({
    Name         = "Display FPS & Server Ping",
    CurrentValue = false,
    Flag         = "SettingsDisplayFPS",
    Callback     = function(Value) end,
})

-- // WEBHOOK ROLL LISTENER

task.spawn(function()
    local sentKeys = {}
    local lastFingerprint = nil

    local function sendWebhook(pName, displayName, rarity, chanceText, statsText, muts, mutList, img, rolls, coins, kills)
        local url = Rayfield.Flags.WebhookURL.CurrentValue
        if not url or url == "" then return end

        local uid  = Rayfield.Flags.WebhookUserID.CurrentValue
        local ping = (uid and uid ~= "") and ("<@"..uid.."> ") or ""

        local finalImg = nil
        if img and img ~= "N/A" then
            local aid = string.match(img, "rbxassetid://(%d+)")
            if aid then finalImg = getThumbnailUrl(aid) end
        end

        local sz = getMutationSize(muts)
        local hidden = "||"..pName.."||"
        local desc = string.format("**%s** rolled **%s**!\n\nðŸŽ² **Total Rolls:** %s", hidden, displayName, ordinal(rolls))

        local fields = {
            {name = "Rarity", value = rarity,     inline = true},
            {name = "Chance", value = chanceText, inline = true},
        }
        if statsText and statsText ~= "" then
            table.insert(fields, {name = "Stats", value = statsText, inline = true})
        end
        if mutList and #mutList > 0 then
            table.insert(fields, {name = "Mutations", value = table.concat(mutList, ", "), inline = true})
        end
        table.insert(fields, {name = "ðŸª™ Coins",  value = formatNumber(coins), inline = true})
        table.insert(fields, {name = "âš”ï¸ Kills",  value = formatNumber(kills), inline = true})

        local payload = {
            content  = ping,
            username = "Game Hub",
            embeds   = {{
                title       = "New Slime Rolled!",
                description = desc,
                thumbnail   = finalImg and {url = finalImg, width = sz, height = sz} or nil,
                fields      = fields,
                color       = getMutationColor(muts),
            }}
        }

        pcall(function()
            request({
                Url     = url,
                Method  = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body    = http:JSONEncode(payload)
            })
        end)
    end

    while true do
        task.wait(0.1)

        local url = Rayfield.Flags.WebhookURL.CurrentValue
        if not url or url == "" then continue end

        local ok, res = pcall(rollSlice.rollResults)
        if not ok then continue end

        local fp = getRollFingerprint(res)
        if fp == lastFingerprint then continue end
        lastFingerprint = fp

        if type(res) ~= "table" or #res == 0 then continue end

        local stats  = dataCli:get("stats") or {}
        local rolls  = stats.rolls or 0
        local kills  = stats.kills or 0
        local coins  = dataCli:get("coins") or 0
        local minLuck = parseLuck(Rayfield.Flags.WebhookMinLuck.CurrentValue)
        local allowedRarities = Rayfield.Flags.WebhookRarity.CurrentOption or {}

        for _, r in ipairs(res) do
            local it = getFirstItem(r)
            if not it then continue end

            local id = tostring(it.id or "unknown")
            local okSlime, slime = pcall(slimeData.getSlime, id)
            slime = okSlime and slime or nil

            local muts = type(it.mutations) == "table" and next(it.mutations) ~= nil and it.mutations or nil
            local isInverted = muts and it.mutations.inverted == true
            local isShiny    = muts and it.mutations.shiny == true
            local isBig      = muts and it.mutations.big == true
            local isHuge     = muts and it.mutations.huge == true
            local hasMutation = muts ~= nil

            -- Filter: Send Inverted toggle
            if isInverted and not Rayfield.Flags.WebhookSendInverted.CurrentValue then continue end

            -- Filter: Send Mutations toggle â€” if OFF, skip anything with a mutation (shiny/big/huge/inverted)
            -- If ON, mutations are allowed through (rarity filter still applies)
            if hasMutation and not isInverted and not Rayfield.Flags.WebhookSendMutations.CurrentValue then continue end

            local img = isInverted and (slime and slime.invertedIcon or "N/A") or (slime and slime.image or "N/A")
            local name = slime and slime.name or id

            local baseOdds  = slime and slime.odds or nil
            local baseDmg   = slime and slime.damage or 0
            local baseHp    = slime and slime.health or 0

            local mult      = muts and mutations.getVisualOddsMultiplier(muts) or 1
            local statBonus = muts and mutations.getStatBonus(muts, "damage") or 1
            local finalOdds = baseOdds and (baseOdds / mult) or nil
            local finalDmg  = baseDmg * statBonus
            local finalHp   = baseHp * statBonus
            local chanceNum = finalOdds and (1 / finalOdds) or nil

            -- Filter: Minimum Luck
            if minLuck > 0 and chanceNum and chanceNum < minLuck then continue end

            local rarity      = getRarityFromOdds(baseOdds)
            local chanceText  = finalOdds and string.format("1 in %s", formatNumber(math.floor(1 / finalOdds + 0.5))) or "N/A"
            local displayName = muts and mutations.getDisplayName(name, muts) or name
            local mutList     = muts and mutations.getIds(muts) or {}

            -- Filter: Rarity â€” must be in the selected list
            local rarityAllowed = false
            for _, v in ipairs(allowedRarities) do
                if v == rarity then rarityAllowed = true break end
            end
            if not rarityAllowed then continue end

            local statsText = ""
            if finalDmg > 0 and finalHp > 0 then
                statsText = string.format("âš”ï¸ %s  â¤ï¸ %s", formatNumber(finalDmg), formatNumber(finalHp))
            elseif finalDmg > 0 then
                statsText = string.format("âš”ï¸ %s", formatNumber(finalDmg))
            elseif finalHp > 0 then
                statsText = string.format("â¤ï¸ %s", formatNumber(finalHp))
            end

            local pName = localPlr and localPlr.Name or "Someone"
            local key = pName.."_"..displayName.."_"..tostring(os.time())
            if not sentKeys[key] then
                sentKeys[key] = os.time()
                task.spawn(sendWebhook, pName, displayName, rarity, chanceText, statsText, muts, mutList, img, rolls, coins, kills)
            end
        end
    end
end)

-- // ANTI AFK BOOT (runs on load since it's enabled by default)
do
    local VirtualUser = game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:Connect(function()
        if Rayfield.Flags.SettingsAntiAFK and Rayfield.Flags.SettingsAntiAFK.CurrentValue then
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end
    end)
end

Rayfield:LoadConfiguration()
