local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local function tween(instance, info, props)
    local t = TweenService:Create(instance, info, props)
    t:Play()
    return t
end

local function setTransparency(parent, value)
    for _, child in ipairs(parent:GetDescendants()) do
        if child:IsA("Frame") or child:IsA("ScrollingFrame") then
            child.BackgroundTransparency = value
        elseif child:IsA("TextLabel") then
            child.TextTransparency = value
            child.BackgroundTransparency = value
        elseif child:IsA("ImageLabel") then
            child.ImageTransparency = value
            child.BackgroundTransparency = value
        elseif child:IsA("UIStroke") then
            child.Transparency = value
        end
    end
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "EggGalleryGui"
screenGui.DisplayOrder = 999
screenGui.IgnoreGuiInset = true
screenGui.ResetOnSpawn = false
screenGui.Parent = PlayerGui

local backdrop = Instance.new("Frame")
backdrop.Name = "Backdrop"
backdrop.Size = UDim2.new(1, 0, 1, 0)
backdrop.Position = UDim2.new(0, 0, 0, 0)
backdrop.BackgroundColor3 = Color3.fromRGB(8, 8, 14)
backdrop.BorderSizePixel = 0
backdrop.ZIndex = 1
backdrop.Parent = screenGui

local loadCard = Instance.new("Frame")
loadCard.Name = "LoadCard"
loadCard.AnchorPoint = Vector2.new(0.5, 0.5)
loadCard.Size = UDim2.new(0, 280, 0, 100)
loadCard.Position = UDim2.new(0.5, 0, 0.5, 0)
loadCard.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
loadCard.BorderSizePixel = 0
loadCard.ZIndex = 2
loadCard.Parent = screenGui

local loadCorner = Instance.new("UICorner")
loadCorner.CornerRadius = UDim.new(0, 16)
loadCorner.Parent = loadCard

local loadStroke = Instance.new("UIStroke")
loadStroke.Color = Color3.fromRGB(90, 90, 140)
loadStroke.Thickness = 1.5
loadStroke.Transparency = 0.45
loadStroke.Parent = loadCard

local loadLabel = Instance.new("TextLabel")
loadLabel.Name = "LoadLabel"
loadLabel.Size = UDim2.new(1, 0, 1, 0)
loadLabel.BackgroundTransparency = 1
loadLabel.Text = "Loading"
loadLabel.TextColor3 = Color3.fromRGB(220, 220, 235)
loadLabel.TextSize = 26
loadLabel.Font = Enum.Font.GothamBold
loadLabel.TextXAlignment = Enum.TextXAlignment.Center
loadLabel.TextYAlignment = Enum.TextYAlignment.Center
loadLabel.ZIndex = 3
loadLabel.Parent = loadCard

local pulseTween = tween(
    loadLabel,
    TweenInfo.new(0.9, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true),
    { TextTransparency = 0.55 }
)

local ItemsModule = ReplicatedStorage:WaitForChild("Tables"):WaitForChild("Items")
local Items = require(ItemsModule)

local collected = {}

for name, data in pairs(Items) do
    if data.Type == "Egg" and data.Rarity == "Exclusive" then
        local iconId = data.Icon and data.Icon:match("rbxassetid://(%d+)")
        if iconId then
            table.insert(collected, { name = name, iconId = iconId })
        end
    end
end

table.sort(collected, function(a, b) return a.name < b.name end)

pulseTween:Cancel()
loadLabel.TextTransparency = 0
loadLabel.Text = "Loaded"

task.wait(0.75)

local FADE_OUT = TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

tween(loadCard, FADE_OUT, { BackgroundTransparency = 1 })
tween(loadStroke, FADE_OUT, { Transparency = 1 })
tween(loadLabel, FADE_OUT, { TextTransparency = 1 })

task.wait(0.65)
loadCard:Destroy()

local header = Instance.new("Frame")
header.Name = "Header"
header.Size = UDim2.new(1, 0, 0, 56)
header.Position = UDim2.new(0, 0, 0, 0)
header.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
header.BorderSizePixel = 0
header.ZIndex = 2
header.Parent = screenGui

local headerStroke = Instance.new("UIStroke")
headerStroke.Color = Color3.fromRGB(70, 70, 110)
headerStroke.Thickness = 1
headerStroke.Transparency = 0.55
headerStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
headerStroke.Parent = header

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(1, -24, 1, 0)
titleLabel.Position = UDim2.new(0, 20, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Exclusive Eggs"
titleLabel.TextColor3 = Color3.fromRGB(240, 240, 255)
titleLabel.TextSize = 20
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.TextYAlignment = Enum.TextYAlignment.Center
titleLabel.ZIndex = 3
titleLabel.Parent = header

local countLabel = Instance.new("TextLabel")
countLabel.Name = "Count"
countLabel.Size = UDim2.new(0, 120, 1, 0)
countLabel.Position = UDim2.new(1, -130, 0, 0)
countLabel.BackgroundTransparency = 1
countLabel.Text = #collected .. " items"
countLabel.TextColor3 = Color3.fromRGB(130, 130, 180)
countLabel.TextSize = 14
countLabel.Font = Enum.Font.Gotham
countLabel.TextXAlignment = Enum.TextXAlignment.Right
countLabel.TextYAlignment = Enum.TextYAlignment.Center
countLabel.ZIndex = 3
countLabel.Parent = header

local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Name = "ScrollFrame"
scrollFrame.Size = UDim2.new(1, 0, 1, -56)
scrollFrame.Position = UDim2.new(0, 0, 0, 56)
scrollFrame.BackgroundTransparency = 1
scrollFrame.BorderSizePixel = 0
scrollFrame.ScrollBarThickness = 6
scrollFrame.ScrollBarImageColor3 = Color3.fromRGB(90, 90, 150)
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
scrollFrame.ZIndex = 2
scrollFrame.Parent = screenGui

local grid = Instance.new("UIGridLayout")
grid.CellSize = UDim2.new(0.22, 0, 0, 0)
grid.CellPadding = UDim2.new(0.02, 0, 0, 12)
grid.FillDirection = Enum.FillDirection.Horizontal
grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
grid.VerticalAlignment = Enum.VerticalAlignment.Top
grid.SortOrder = Enum.SortOrder.LayoutOrder
grid.Parent = scrollFrame

local function updateCellHeight()
    local colWidth = math.floor(scrollFrame.AbsoluteSize.X * 0.22)
    grid.CellSize = UDim2.new(0.22, 0, 0, colWidth + 28)
end

scrollFrame:GetPropertyChangedSignal("AbsoluteSize"):Connect(updateCellHeight)
updateCellHeight()

local gridPad = Instance.new("UIPadding")
gridPad.PaddingTop = UDim.new(0, 16)
gridPad.PaddingBottom = UDim.new(0, 24)
gridPad.PaddingLeft = UDim.new(0, 16)
gridPad.PaddingRight = UDim.new(0, 16)
gridPad.Parent = scrollFrame

for i, item in ipairs(collected) do
    local card = Instance.new("Frame")
    card.Name = "Card_" .. item.name
    card.BackgroundColor3 = Color3.fromRGB(20, 20, 32)
    card.BorderSizePixel = 0
    card.LayoutOrder = i
    card.ZIndex = 3
    card.Parent = scrollFrame

    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 12)
    cardCorner.Parent = card

    local cardStroke = Instance.new("UIStroke")
    cardStroke.Color = Color3.fromRGB(70, 70, 110)
    cardStroke.Thickness = 1
    cardStroke.Transparency = 0.5
    cardStroke.Parent = card

    local icon = Instance.new("ImageLabel")
    icon.Name = "Icon"
    icon.Size = UDim2.new(1, -16, 0.72, 0)
    icon.Position = UDim2.new(0, 8, 0, 8)
    icon.BackgroundTransparency = 1
    icon.Image = "rbxassetid://" .. item.iconId
    icon.ScaleType = Enum.ScaleType.Fit
    icon.ZIndex = 4
    icon.Parent = card

    local nameLabel = Instance.new("TextLabel")
    nameLabel.Name = "NameLabel"
    nameLabel.Size = UDim2.new(1, -8, 0.24, 0)
    nameLabel.Position = UDim2.new(0, 4, 0.75, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = item.name
    nameLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
    nameLabel.TextSize = 12
    nameLabel.Font = Enum.Font.Gotham
    nameLabel.TextXAlignment = Enum.TextXAlignment.Center
    nameLabel.TextYAlignment = Enum.TextYAlignment.Center
    nameLabel.TextTruncate = Enum.TextTruncate.AtEnd
    nameLabel.ZIndex = 4
    nameLabel.Parent = card

    card.BackgroundTransparency = 1
    icon.ImageTransparency = 1
    nameLabel.TextTransparency = 1
    cardStroke.Transparency = 1

    local delay = (i - 1) * 0.045
    local FADE_IN = TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    task.delay(delay, function()
        tween(card, FADE_IN, { BackgroundTransparency = 0 })
        tween(icon, FADE_IN, { ImageTransparency = 0 })
        tween(nameLabel, FADE_IN, { TextTransparency = 0 })
        tween(cardStroke, FADE_IN, { Transparency = 0.5 })
    end)
end

header.BackgroundTransparency = 1
headerStroke.Transparency = 1
titleLabel.TextTransparency = 1
countLabel.TextTransparency = 1

local HDR_FADE = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
tween(header, HDR_FADE, { BackgroundTransparency = 0 })
tween(titleLabel, HDR_FADE, { TextTransparency = 0 })
tween(countLabel, HDR_FADE, { TextTransparency = 0 })
tween(headerStroke, HDR_FADE, { Transparency = 0.55 })

tween(backdrop, TweenInfo.new(0.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
    { BackgroundColor3 = Color3.fromRGB(10, 10, 18) })
