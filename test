local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Slime Hub",
    SubTitle = "by Malik",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Main     = Window:AddTab({ Title = "Main",    Icon = "star" }),
    Game     = Window:AddTab({ Title = "Game",    Icon = "zap" }),
    Misc     = Window:AddTab({ Title = "Misc",    Icon = "layers" }),
    Rewards  = Window:AddTab({ Title = "Rewards", Icon = "gift" }),
    Settings = Window:AddTab({ Title = "Settings",Icon = "settings" }),
}

-- ─── Main ────────────────────────────────────────────────────────────────────

Tabs.Main:AddToggle("BestZone", {
    Title       = "Stay in Best Unlocked Zone",
    Description = "Automatically moves you to the best zone you have unlocked.",
    Default     = false,
})

Tabs.Main:AddToggle("UnlockZones", {
    Title       = "Unlock Affordable Zones",
    Description = "Automatically unlocks zones you can afford.",
    Default     = false,
})

Tabs.Main:AddToggle("EquipBestSlimes", {
    Title       = "Equip Best Slimes",
    Description = "Automatically equips your highest-tier slimes.",
    Default     = false,
})

Tabs.Main:AddToggle("FastRoll", {
    Title       = "Fast Roll",
    Description = "Increases roll speed to the maximum allowed rate.",
    Default     = false,
})

Tabs.Main:AddInput("WebhookURL", {
    Title       = "Webhook URL",
    Description = "Discord webhook URL to send notifications to.",
    Placeholder = "https://discord.com/api/webhooks/...",
    Default     = "",
})

Tabs.Main:AddInput("MinLuck", {
    Title       = "Minimum Luck to Send",
    Description = "Only send a webhook notification when luck is at or above this value.",
    Placeholder  = "e.g. 1000",
    Default     = "",
    Numeric     = true,
})

-- ─── Game ─────────────────────────────────────────────────────────────────────

Tabs.Game:AddToggle("AutoRebirth", {
    Title       = "Rebirth",
    Description = "Automatically rebirths when the requirement is met.",
    Default     = false,
})

Tabs.Game:AddToggle("AutoUpgrade", {
    Title       = "Upgrade Purchasing",
    Description = "Automatically purchases upgrades when affordable.",
    Default     = false,
})

Tabs.Game:AddInput("SlimeSpeed", {
    Title       = "Super Fast Slime Speed",
    Description = "Set a custom slime movement speed multiplier.",
    Placeholder = "e.g. 50",
    Default     = "",
    Numeric     = true,
})

Tabs.Game:AddInput("EnemySpeed", {
    Title       = "Super Fast Enemies",
    Description = "Set a custom enemy movement speed multiplier.",
    Placeholder = "e.g. 50",
    Default     = "",
    Numeric     = true,
})

-- ─── Misc ─────────────────────────────────────────────────────────────────────

Tabs.Misc:AddToggle("RedeemCodes", {
    Title       = "Redeem Codes",
    Description = "Automatically redeems all available promo codes.",
    Default     = false,
})

Tabs.Misc:AddToggle("UsePotions", {
    Title       = "Use Potions",
    Description = "Automatically uses potions from your inventory.",
    Default     = false,
})

Tabs.Misc:AddToggle("UseDice", {
    Title       = "Use Dice / Items",
    Description = "Automatically uses dice and other consumable items.",
    Default     = false,
})

Tabs.Misc:AddToggle("FriendBoost", {
    Title       = "Maximise Friend Boost",
    Description = "Keeps friend boost slots filled for maximum bonus.",
    Default     = false,
})

-- ─── Rewards ──────────────────────────────────────────────────────────────────

Tabs.Rewards:AddToggle("OfflineEarnings", {
    Title       = "Offline Earnings Claim",
    Description = "Automatically claims offline earnings on login.",
    Default     = false,
})

Tabs.Rewards:AddToggle("IndexRewards", {
    Title       = "Index Rewards Claim",
    Description = "Automatically claims index milestone rewards.",
    Default     = false,
})

Tabs.Rewards:AddToggle("LootCollection", {
    Title       = "Loot Collection",
    Description = "Automatically collects dropped loot on the map.",
    Default     = false,
})

-- ─── Settings ─────────────────────────────────────────────────────────────────

Tabs.Settings:AddToggle("AntiAFK", {
    Title       = "Anti AFK",
    Description = "Prevents the game from detecting you as AFK.",
    Default     = false,
})

Tabs.Settings:AddToggle("AntiKick", {
    Title       = "Anti Kick",
    Description = "Reduces the chance of being kicked from the server.",
    Default     = false,
})

Tabs.Settings:AddToggle("Optimization", {
    Title       = "Optimization",
    Description = "Applies performance tweaks to reduce lag.",
    Default     = false,
})

-- ─── Save / Interface managers ────────────────────────────────────────────────

SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)

InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

SaveManager:LoadAutoloadConfig()
