--[[
    Recreated UI from PlayerGui dump.
    Script creates a UI programmatically without referencing original instances.
    Includes all ScreenGuis: TopbarCenteredClipped, TopbarStandardClipped, TopbarStandard.
    Interactive elements: TextButton (ClickRegion) inside IconSpot.
    Layout preserved with UIListLayout, UICorner, UIPadding, etc.
    Missing properties use reasonable defaults.
]]

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Helper function to create a Color3 from RGB fractions
local function Color3FromFraction(r,g,b)
    return Color3.new(r,g,b)
end

-- Common colors used in the dump
local grayColor = Color3FromFraction(0.639216, 0.635294, 0.647059)
local darkColor = Color3FromFraction(0.152941, 0.160784, 0.188235)
local veryDarkColor = Color3FromFraction(0.0705882, 0.0705882, 0.0823529)
local lightColor = Color3FromFraction(0.882353, 0.882353, 0.882353)
local whiteColor = Color3.new(1,1,1)
local blackColor = Color3.new(0,0,0)

-----------------------------------------------------------
-- 1. TopbarCenteredClipped
-----------------------------------------------------------
do
    local screenGuiCentered = Instance.new("ScreenGui")
    screenGuiCentered.Name = "TopbarCenteredClipped"
    screenGuiCentered.Parent = playerGui

    local holdersCentered = Instance.new("Frame")
    holdersCentered.Name = "Holders"
    holdersCentered.Size = UDim2.new(1,0,0,50)
    holdersCentered.Position = UDim2.new(0,0,0,0)
    holdersCentered.BackgroundColor3 = grayColor
    holdersCentered.Parent = screenGuiCentered
end

-----------------------------------------------------------
-- 2. TopbarStandardClipped
-----------------------------------------------------------
do
    local screenGuiClipped = Instance.new("ScreenGui")
    screenGuiClipped.Name = "TopbarStandardClipped"
    screenGuiClipped.Parent = playerGui

    local holdersClipped = Instance.new("Frame")
    holdersClipped.Name = "Holders"
    holdersClipped.Size = UDim2.new(0,0,0,0)
    holdersClipped.Position = UDim2.new(0,0,0,0)
    holdersClipped.BackgroundColor3 = grayColor
    holdersClipped.Parent = screenGuiClipped

    local clippedContainer = Instance.new("Folder")
    clippedContainer.Name = "ClippedContainer"
    clippedContainer.Parent = holdersClipped

    -- Function to create a Caption CanvasGroup (used twice: DPadLeft, DPadRight)
    local function createCaption(headerText, keyText, yOffset)
        local caption = Instance.new("CanvasGroup")
        caption.Name = "Caption"
        caption.Size = UDim2.new(0,0,0,0)
        caption.Position = UDim2.new(0,12,0,16 + yOffset)
        caption.BackgroundColor3 = grayColor
        caption.Parent = clippedContainer

        local box = Instance.new("Frame")
        box.Name = "Box"
        box.Size = UDim2.new(0,0,0,0)
        box.Position = UDim2.new(0,4,0,7)
        box.BackgroundColor3 = darkColor
        box.Parent = caption

        local header = Instance.new("TextLabel")
        header.Name = "Header"
        header.Size = UDim2.new(0,0,0,16)
        header.Position = UDim2.new(0,0,0,0)
        header.Text = headerText
        header.BackgroundColor3 = grayColor
        header.Parent = box

        local layout = Instance.new("UIListLayout")
        layout.Name = "Layout"
        layout.Parent = box

        local captionCorner = Instance.new("UICorner")
        captionCorner.Name = "CaptionCorner"
        captionCorner.Parent = box

        local padding = Instance.new("UIPadding")
        padding.Name = "Padding"
        padding.Parent = box

        local hotkeys = Instance.new("Frame")
        hotkeys.Name = "Hotkeys"
        hotkeys.Size = UDim2.new(1,0,0,0)
        hotkeys.Position = UDim2.new(0,0,0,0)
        hotkeys.BackgroundColor3 = grayColor
        hotkeys.Parent = box

        local layout1 = Instance.new("UIListLayout")
        layout1.Name = "Layout1"
        layout1.Parent = hotkeys

        local key1 = Instance.new("ImageLabel")
        key1.Name = "Key1"
        key1.Size = UDim2.new(0,0,0,30)
        key1.Position = UDim2.new(0,0,0,0)
        key1.Image = "rbxasset://textures/ui/Controls/key_single.png"
        key1.BackgroundColor3 = grayColor
        key1.Parent = hotkeys

        local inset = Instance.new("UIPadding")
        inset.Name = "Inset"
        inset.Parent = key1

        local labelContent = Instance.new("TextLabel")
        labelContent.Name = "LabelContent"
        labelContent.Size = UDim2.new(1,0,1,0)
        labelContent.Position = UDim2.new(0,0,0,-1)
        labelContent.Text = keyText
        labelContent.BackgroundColor3 = grayColor
        labelContent.Parent = key1

        -- Caret
        local caret = Instance.new("ImageLabel")
        caret.Name = "Caret"
        caret.Size = UDim2.new(0,16,0,8)
        caret.Position = UDim2.new(0,0,0,4)
        caret.Image = "rbxassetid://101906294438076"
        caret.BackgroundColor3 = grayColor
        caret.Parent = caption

        -- DropShadow
        local dropShadow = Instance.new("ImageLabel")
        dropShadow.Name = "DropShadow"
        dropShadow.Size = UDim2.new(1,0,0,62)
        dropShadow.Position = UDim2.new(0,0,0,5)
        dropShadow.Image = "rbxassetid://124920646932671"
        dropShadow.BackgroundColor3 = grayColor
        dropShadow.Parent = caption

        -- ClippedClone ObjectValue
        local clippedClone = Instance.new("ObjectValue")
        clippedClone.Name = "ClippedClone"
        clippedClone.Parent = caption
    end

    -- First Caption: DPadLeft (yOffset = 0)
    createCaption("_hotkey_", "DPadLeft", 0)

    -- Second Caption: DPadRight (yOffset = 1490)
    createCaption("_hotkey_", "DPadRight", 1490)
end

-----------------------------------------------------------
-- 3. TopbarStandard (Main Interface)
-----------------------------------------------------------
do
    local screenGuiStandard = Instance.new("ScreenGui")
    screenGuiStandard.Name = "TopbarStandard"
    screenGuiStandard.Parent = playerGui

    local holders = Instance.new("Frame")
    holders.Name = "Holders"
    holders.Size = UDim2.new(1,0,1,-2)
    holders.Position = UDim2.new(0,0,0,0)
    holders.BackgroundColor3 = grayColor
    holders.Parent = screenGuiStandard

    -- Left ScrollingFrame
    local leftScroll = Instance.new("ScrollingFrame")
    leftScroll.Name = "Left"
    leftScroll.Size = UDim2.new(1,-24,1,0)
    leftScroll.Position = UDim2.new(0,8,0,0)
    leftScroll.BackgroundColor3 = grayColor
    leftScroll.Parent = holders

    local leftListLayout = Instance.new("UIListLayout")
    leftListLayout.Name = "UIListLayout"
    leftListLayout.Parent = leftScroll

    -- Function to create a Widget (IconButton + Menu + ClickRegion)
    local function createWidget(iconImage, hasIndicator, indicatorImage, hasCaption, yOffset, parent)
        local widget = Instance.new("Frame")
        widget.Name = "Widget"
        widget.Size = UDim2.new(0,44,0,44)
        widget.Position = UDim2.new(0,0,0,0)
        widget.BackgroundColor3 = grayColor
        widget.Parent = parent

        local iconButton = Instance.new("Frame")
        iconButton.Name = "IconButton"
        iconButton.Size = UDim2.new(1,0,1,0)
        iconButton.Position = UDim2.new(0,0,0,0)
        iconButton.BackgroundColor3 = veryDarkColor
        iconButton.Parent = widget

        local uiCorner = Instance.new("UICorner")
        uiCorner.Name = "UICorner"
        uiCorner.Parent = iconButton

        local menu = Instance.new("ScrollingFrame")
        menu.Name = "Menu"
        menu.Size = UDim2.new(1,0,1,0)
        menu.Position = UDim2.new(0,0,0,0) -- will adjust later for right side
        menu.BackgroundColor3 = grayColor
        menu.Parent = iconButton

        local menuLayout = Instance.new("UIListLayout")
        menuLayout.Name = "MenuUIListLayout"
        menuLayout.Parent = menu

        local menuGap = Instance.new("Frame")
        menuGap.Name = "MenuGap"
        menuGap.Size = UDim2.new(0,4,0,0)
        menuGap.Position = UDim2.new(0,0,0,0)
        menuGap.BackgroundColor3 = grayColor
        menuGap.Parent = menu

        local iconSpot = Instance.new("Frame")
        iconSpot.Name = "IconSpot"
        iconSpot.Size = UDim2.new(0,36,1,-8)
        iconSpot.Position = UDim2.new(0,4,0.5,0)
        iconSpot.BackgroundColor3 = grayColor -- will be overwritten for some
        iconSpot.Parent = menu

        local spotCorner = Instance.new("UICorner")
        spotCorner.Name = "UICorner"
        spotCorner.Parent = iconSpot

        local iconOverlay = Instance.new("Frame")
        iconOverlay.Name = "IconOverlay"
        iconOverlay.Size = UDim2.new(1,0,1,0)
        iconOverlay.Position = UDim2.new(0,0,0,0)
        iconOverlay.BackgroundColor3 = whiteColor
        iconOverlay.Parent = iconSpot

        local overlayCorner = Instance.new("UICorner")
        overlayCorner.Name = "OverlayUICorner"
        overlayCorner.Parent = iconOverlay

        local clickRegion = Instance.new("TextButton")
        clickRegion.Name = "ClickRegion"
        clickRegion.Size = UDim2.new(0,36,1,0)
        clickRegion.Position = UDim2.new(0,0,0,0)
        clickRegion.Text = ""
        clickRegion.BackgroundColor3 = grayColor
        clickRegion.Parent = iconSpot

        local clickCorner = Instance.new("UICorner")
        clickCorner.Name = "UICorner"
        clickCorner.Parent = clickRegion

        if hasCaption then
            local caption = Instance.new("Frame")
            caption.Name = "Caption"
            caption.Size = UDim2.new(0,0,0,0)
            caption.Position = UDim2.new(0,0,0,0)
            caption.BackgroundColor3 = grayColor
            caption.Parent = clickRegion

            local originalInstance = Instance.new("ObjectValue")
            originalInstance.Name = "OriginalInstance"
            originalInstance.Parent = caption
        end

        -- Contents frame
        local contents = Instance.new("Frame")
        contents.Name = "Contents"
        contents.Size = UDim2.new(1,0,1,0)
        contents.Position = UDim2.new(0,0,0,0)
        contents.BackgroundColor3 = grayColor
        contents.Parent = iconSpot

        local contentsList = Instance.new("UIListLayout")
        contentsList.Name = "ContentsList"
        contentsList.Parent = contents

        -- Padding frames (left, center, right)
        local paddingLeft = Instance.new("Frame")
        paddingLeft.Name = "PaddingLeft"
        paddingLeft.Size = UDim2.new(0,9,1,0)
        paddingLeft.Position = UDim2.new(0,0,0,0)
        paddingLeft.BackgroundColor3 = whiteColor
        paddingLeft.Parent = contents

        local paddingCenter = Instance.new("Frame")
        paddingCenter.Name = "PaddingCenter"
        paddingCenter.Size = UDim2.new(0,0,1,0)
        paddingCenter.Position = UDim2.new(0,0,0,0)
        paddingCenter.BackgroundColor3 = whiteColor
        paddingCenter.Parent = contents

        local paddingRight = Instance.new("Frame")
        paddingRight.Name = "PaddingRight"
        paddingRight.Size = UDim2.new(0,11,1,0)
        paddingRight.Position = UDim2.new(0,0,0,0)
        paddingRight.BackgroundColor3 = whiteColor
        paddingRight.Parent = contents

        -- IconLabelContainer
        local iconLabelContainer = Instance.new("Frame")
        iconLabelContainer.Name = "IconLabelContainer"
        iconLabelContainer.Size = UDim2.new(0,0,1,0)
        iconLabelContainer.Position = UDim2.new(0.5,0,0.5,0)
        iconLabelContainer.BackgroundColor3 = grayColor
        iconLabelContainer.Parent = contents

        local iconLabel = Instance.new("TextLabel")
        iconLabel.Name = "IconLabel"
        iconLabel.Size = UDim2.new(0,1906,1,0)
        iconLabel.Position = UDim2.new(0,0,0,0)
        iconLabel.Text = ""
        iconLabel.BackgroundColor3 = grayColor
        iconLabel.Parent = iconLabelContainer

        -- IconImage
        local iconImage = Instance.new("ImageLabel")
        iconImage.Name = "IconImage"
        iconImage.Size = UDim2.new(0.600000024,0,0.600000024,0)
        iconImage.Position = UDim2.new(0,11,0.5,0)
        if iconImage ~= "" then
            iconImage.Image = iconImage
        else
            iconImage.Image = ""
        end
        iconImage.BackgroundColor3 = grayColor
        iconImage.Parent = contents

        local iconImageCorner = Instance.new("UICorner")
        iconImageCorner.Name = "IconImageCorner"
        iconImageCorner.Parent = iconImage

        local iconImageScale = Instance.new("NumberValue")
        iconImageScale.Name = "IconImageScale"
        iconImageScale.Parent = iconImage

        local iconImageRatio = Instance.new("UIAspectRatioConstraint")
        iconImageRatio.Name = "IconImageRatio"
        iconImageRatio.Parent = iconImage

        -- Indicator (optional)
        if hasIndicator then
            local indicator = Instance.new("Frame")
            indicator.Name = "Indicator"
            indicator.Size = UDim2.new(0,42,0,42)
            indicator.Position = UDim2.new(1,0,0.5,0)
            indicator.BackgroundColor3 = blackColor
            indicator.Parent = iconSpot

            local indicatorButton = Instance.new("Frame")
            indicatorButton.Name = "IndicatorButton"
            indicatorButton.Size = UDim2.new(1,0,1,0)
            indicatorButton.Position = UDim2.new(0.5,2,0.5,0)
            indicatorButton.BackgroundColor3 = blackColor
            indicatorButton.Parent = indicator

            local indicatorImage = Instance.new("ImageLabel")
            indicatorImage.Name = "ImageLabel"
            indicatorImage.Size = UDim2.new(0.5,0,0.5,0)
            indicatorImage.Position = UDim2.new(0.5,0,0.5,0)
            indicatorImage.Image = indicatorImage
            indicatorImage.BackgroundColor3 = grayColor
            indicatorImage.Parent = indicatorButton

            local indicatorCorner = Instance.new("UICorner")
            indicatorCorner.Name = "UICorner"
            indicatorCorner.Parent = indicatorButton
        end

        -- IconSpotGradient
        local spotGradient = Instance.new("UIGradient")
        spotGradient.Name = "IconSpotGradient"
        spotGradient.Parent = iconSpot

        -- IconGradient on IconButton
        local iconGradient = Instance.new("UIGradient")
        iconGradient.Name = "IconGradient"
        iconGradient.Parent = iconButton
    end

    -- First Widget (Left) with Indicator (DPadUp)
    createWidget("rbxassetid://108054639083420", true, "rbxasset://textures/ui/Controls/XboxController/DPadUp@2x.png", false, 0, leftScroll)

    -- OverflowLeft widget
    local overflowLeft = Instance.new("Frame")
    overflowLeft.Name = "OverflowLeft"
    overflowLeft.Size = UDim2.new(0,55,0,44)
    overflowLeft.Position = UDim2.new(0,0,0,0)
    overflowLeft.BackgroundColor3 = grayColor
    overflowLeft.Parent = leftScroll

    -- Create a similar Widget structure for OverflowLeft (simplified, no indicator)
    local overflowIconButton = Instance.new("Frame")
    overflowIconButton.Name = "IconButton"
    overflowIconButton.Size = UDim2.new(1,0,1,0)
    overflowIconButton.Position = UDim2.new(0,0,0,0)
    overflowIconButton.BackgroundColor3 = veryDarkColor
    overflowIconButton.Parent = overflowLeft

    local overflowCorner = Instance.new("UICorner")
    overflowCorner.Name = "UICorner"
    overflowCorner.Parent = overflowIconButton

    local overflowMenu = Instance.new("ScrollingFrame")
    overflowMenu.Name = "Menu"
    overflowMenu.Size = UDim2.new(1,0,1,0)
    overflowMenu.Position = UDim2.new(0,0,0,0)
    overflowMenu.BackgroundColor3 = grayColor
    overflowMenu.Parent = overflowIconButton

    local overflowMenuLayout = Instance.new("UIListLayout")
    overflowMenuLayout.Name = "MenuUIListLayout"
    overflowMenuLayout.Parent = overflowMenu

    local overflowMenuGap = Instance.new("Frame")
    overflowMenuGap.Name = "MenuGap"
    overflowMenuGap.Size = UDim2.new(0,4,0,0)
    overflowMenuGap.Position = UDim2.new(0,0,0,0)
    overflowMenuGap.BackgroundColor3 = grayColor
    overflowMenuGap.Parent = overflowMenu

    local overflowIconSpot = Instance.new("Frame")
    overflowIconSpot.Name = "IconSpot"
    overflowIconSpot.Size = UDim2.new(0,47,1,-8)
    overflowIconSpot.Position = UDim2.new(0,4,0.5,0)
    overflowIconSpot.BackgroundColor3 = whiteColor
    overflowIconSpot.Parent = overflowMenu

    local overflowSpotCorner = Instance.new("UICorner")
    overflowSpotCorner.Name = "UICorner"
    overflowSpotCorner.Parent = overflowIconSpot

    local overflowOverlay = Instance.new("Frame")
    overflowOverlay.Name = "IconOverlay"
    overflowOverlay.Size = UDim2.new(1,0,1,0)
    overflowOverlay.Position = UDim2.new(0,0,0,0)
    overflowOverlay.BackgroundColor3 = whiteColor
    overflowOverlay.Parent = overflowIconSpot

    local overflowOverlayCorner = Instance.new("UICorner")
    overflowOverlayCorner.Name = "OverlayUICorner"
    overflowOverlayCorner.Parent = overflowOverlay

    local overflowClick = Instance.new("TextButton")
    overflowClick.Name = "ClickRegion"
    overflowClick.Size = UDim2.new(0,47,1,0)
    overflowClick.Position = UDim2.new(0,0,0,0)
    overflowClick.Text = ""
    overflowClick.BackgroundColor3 = grayColor
    overflowClick.Parent = overflowIconSpot

    local overflowClickCorner = Instance.new("UICorner")
    overflowClickCorner.Name = "UICorner"
    overflowClickCorner.Parent = overflowClick

    -- Contents for overflow
    local overflowContents = Instance.new("Frame")
    overflowContents.Name = "Contents"
    overflowContents.Size = UDim2.new(1,0,1,0)
    overflowContents.Position = UDim2.new(0,0,0,0)
    overflowContents.BackgroundColor3 = grayColor
    overflowContents.Parent = overflowIconSpot

    local overflowContentsList = Instance.new("UIListLayout")
    overflowContentsList.Name = "ContentsList"
    overflowContentsList.Parent = overflowContents

    -- Padding frames
    local overflowPadLeft = Instance.new("Frame")
    overflowPadLeft.Name = "PaddingLeft"
    overflowPadLeft.Size = UDim2.new(0,9,1,0)
    overflowPadLeft.Position = UDim2.new(0,0,0,0)
    overflowPadLeft.BackgroundColor3 = whiteColor
    overflowPadLeft.Parent = overflowContents

    local overflowPadCenter = Instance.new("Frame")
    overflowPadCenter.Name = "PaddingCenter"
    overflowPadCenter.Size = UDim2.new(0,0,1,0)
    overflowPadCenter.Position = UDim2.new(0,0,0,0)
    overflowPadCenter.BackgroundColor3 = whiteColor
    overflowPadCenter.Parent = overflowContents

    local overflowPadRight = Instance.new("Frame")
    overflowPadRight.Name = "PaddingRight"
    overflowPadRight.Size = UDim2.new(0,11,1,0)
    overflowPadRight.Position = UDim2.new(0,0,0,0)
    overflowPadRight.BackgroundColor3 = whiteColor
    overflowPadRight.Parent = overflowContents

    local overflowLabelContainer = Instance.new("Frame")
    overflowLabelContainer.Name = "IconLabelContainer"
    overflowLabelContainer.Size = UDim2.new(0,0,1,0)
    overflowLabelContainer.Position = UDim2.new(0.5,0,0.5,0)
    overflowLabelContainer.BackgroundColor3 = grayColor
    overflowLabelContainer.Parent = overflowContents

    local overflowLabel = Instance.new("TextLabel")
    overflowLabel.Name = "IconLabel"
    overflowLabel.Size = UDim2.new(0,1906,1,0)
    overflowLabel.Position = UDim2.new(0,0,0,0)
    overflowLabel.Text = ""
    overflowLabel.BackgroundColor3 = grayColor
    overflowLabel.Parent = overflowLabelContainer

    local overflowImage = Instance.new("ImageLabel")
    overflowImage.Name = "IconImage"
    overflowImage.Size = UDim2.new(0.5,0,0.5,0)
    overflowImage.Position = UDim2.new(0,11,0.5,0)
    overflowImage.Image = ""
    overflowImage.BackgroundColor3 = grayColor
    overflowImage.Parent = overflowContents

    local overflowImageCorner = Instance.new("UICorner")
    overflowImageCorner.Name = "IconImageCorner"
    overflowImageCorner.Parent = overflowImage

    local overflowImageScale = Instance.new("NumberValue")
    overflowImageScale.Name = "IconImageScale"
    overflowImageScale.Parent = overflowImage

    local overflowImageRatio = Instance.new("UIAspectRatioConstraint")
    overflowImageRatio.Name = "IconImageRatio"
    overflowImageRatio.Parent = overflowImage

    local overflowSpotGradient = Instance.new("UIGradient")
    overflowSpotGradient.Name = "IconSpotGradient"
    overflowSpotGradient.Parent = overflowIconSpot

    local overflowIconGradient = Instance.new("UIGradient")
    overflowIconGradient.Name = "IconGradient"
    overflowIconGradient.Parent = overflowIconButton

    -- Right ScrollingFrame
    local rightScroll = Instance.new("ScrollingFrame")
    rightScroll.Name = "Right"
    rightScroll.Size = UDim2.new(1,-24,1,0)
    rightScroll.Position = UDim2.new(1,-12,0,0)
    rightScroll.BackgroundColor3 = grayColor
    rightScroll.Parent = holders

    local rightListLayout = Instance.new("UIListLayout")
    rightListLayout.Name = "UIListLayout"
    rightListLayout.Parent = rightScroll

    -- First right widget: no indicator, no caption, image = rbxassetid://109774121073504
    createWidget("rbxassetid://109774121073504", false, "", false, 0, rightScroll)

    -- Second right widget: no indicator, has caption, image = rbxassetid://926...
    createWidget("rbxassetid://926", false, "", true, 0, rightScroll)
end

print("UI recreated successfully.")
