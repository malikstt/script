local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local RepLibrary = require(ReplicatedStorage.Modules.RepLibrary)
local ItemsModule = require(ReplicatedStorage.Modules.Items)
local GaragesModule = require(ReplicatedStorage.Modules.Garages)

local Auctions = ReplicatedStorage:WaitForChild("Events"):WaitForChild("Auctions")
local EnterQueue = Auctions:WaitForChild("EnterQueue")
local UpdateCurrentBid = Auctions:WaitForChild("UpdateCurrentBid")
local PlaceBid = Auctions:WaitForChild("PlaceBid")
local DataEvent = ReplicatedStorage:WaitForChild("Events"):WaitForChild("Data"):WaitForChild("GetAll")
local SellAllItems = Auctions:WaitForChild("SellAllItems")
local Pass = Auctions:WaitForChild("Pass")

local AuctionsFolder = workspace:WaitForChild("Debris"):WaitForChild("Auctions")

local BID_CHECK_INTERVAL = 0.1

local currentNextBid = nil
local currentBidder = nil
local processingItemsWon = false
local currentGarageId = nil
local garagesWon = 0
local auctionComplete = false

local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local function debugPrint(...)
    print("[AuctionBot]", ...)
end

local function destroySpecificGuis()
    local items = PlayerGui:FindFirstChild("ItemsWon")
    if items then items:Destroy() end
    local circle = PlayerGui:FindFirstChild("CircleAnimationUI")
    if circle then circle:Destroy() end
end

destroySpecificGuis()

PlayerGui.ChildAdded:Connect(function(child)
    if child.Name == "ItemsWon" or child.Name == "CircleAnimationUI" then
        child:Destroy()
    end
end)

local function tryClick(obj)
    if obj and (obj:IsA("TextButton") or obj:IsA("ImageButton")) then
        pcall(function() obj:Activate() end)
        if getconnections then
            for _, v in pairs(getconnections(obj.MouseButton1Click)) do
                pcall(function() v:Fire() end)
            end
        end
    end
end

local function checkForButton()
    local itemsWon = PlayerGui:FindFirstChild("ItemsWon", true)
    if itemsWon then
        local button = itemsWon:FindFirstChild("Button", true)
        if button then
            tryClick(button)
        end
    end
end

checkForButton()

PlayerGui.DescendantAdded:Connect(function(obj)
    if obj.Name == "Button" then
        task.wait(0.5)
        tryClick(obj)
    end
end)

task.spawn(function()
    while true do
        task.wait(0.25)
        checkForButton()
    end
end)

UpdateCurrentBid.OnClientEvent:Connect(function(data)
    if data then
        currentNextBid = data.nextBid
        currentBidder = data.player
    end
end)

local function canAffordAuction(auction, playerCash, playerS2Cash)
    if auction.Type == "Cash" then
        if playerCash < auction.CostToEnter then
            return false
        end
    elseif auction.Type == "S2Cash" then
        if playerS2Cash < auction.CostToEnter then
            return false
        end
    else
        return false
    end
    
    local requiredBudget = auction.CashNeededToEnter or auction.AuctionBudget or 0
    if requiredBudget > 0 and playerCash < requiredBudget then
        return false
    end
    
    return true
end

local function findBestAffordableAuction()
    local playerCash = LocalPlayer.SaveVars.Cash.Value
    local playerS2Cash = LocalPlayer.SaveVars.S2Cash and LocalPlayer.SaveVars.S2Cash.Value or 0
    
    local bestAuction = nil
    local bestScore = -1
    
    debugPrint("Player Balance - Cash:", playerCash, "S2Cash:", playerS2Cash)
    
    for auctionId, auctionData in pairs(GaragesModule) do
        if auctionData.Finished then
            continue
        end
        
        if auctionData.Limited or auctionData.SerialRotation or auctionData.AirportLevel then
            continue
        end
        
        if auctionData.Type ~= "Cash" and auctionData.Type ~= "S2Cash" then
            continue
        end
        
        if canAffordAuction(auctionData, playerCash, playerS2Cash) then
            local auctionScore = (auctionData.AuctionBudget or 0)
            
            if auctionData.CostToEnter > 0 then
                auctionScore = auctionScore + (auctionData.CostToEnter * 10)
            end
            
            if auctionData.Type == "S2Cash" then
                auctionScore = auctionScore + 100000
            end
            
            if auctionScore > bestScore then
                bestScore = auctionScore
                bestAuction = {
                    id = auctionId,
                    data = auctionData
                }
            end
            
            debugPrint("Can afford:", auctionData.Name, "Budget:", auctionData.AuctionBudget)
        end
    end
    
    return bestAuction
end

local function getPlayerData()
    local success, data = pcall(function() return DataEvent:InvokeServer() end)
    if not success then return nil end
    return data
end

local function getInventorySpace()
    local data = getPlayerData()
    if not data then return 0, 0 end
    local invLevel = data.InventoryLevel or 0
    local carryLevel = data.CarryLevel or 0
    local invSpace = RepLibrary:GetInventorySpace(LocalPlayer, invLevel)
    local carrySpace = RepLibrary:GetCarrySpace(LocalPlayer, carryLevel)
    local invCount = RepLibrary:GetLengthOfInventory(data.Inventory or {})
    return invSpace - invCount, carrySpace
end

local function sortItemsByValue(items)
    local itemList = {}
    for guid, itemData in pairs(items) do
        local itemId = tostring(itemData.itemId)
        local itemInfo = ItemsModule[itemId]
        local price = 0
        if itemInfo then
            price = itemInfo.Price or itemInfo.S2Price or 0
        end
        table.insert(itemList, {
            guid = guid,
            itemId = itemId,
            price = price
        })
    end
    table.sort(itemList, function(a, b) return a.price > b.price end)
    return itemList
end

local function handleItemsWon()
    if processingItemsWon then return false end
    processingItemsWon = true
    debugPrint("Handling items won...")
    
    local data = getPlayerData()
    if not data or not data.ItemsWon then
        processingItemsWon = false
        return false
    end
    
    local itemsWon = data.ItemsWon
    if not itemsWon or next(itemsWon) == nil then
        debugPrint("No items won")
        processingItemsWon = false
        return false
    end
    
    local invSpace, carrySpace = getInventorySpace()
    debugPrint("Inventory space:", invSpace, "Carry space:", carrySpace)
    
    local sortedItems = sortItemsByValue(itemsWon)
    local keepGuids = {}
    local keepCount = 0
    local maxKeep = invSpace + carrySpace
    
    if maxKeep <= 0 then
        debugPrint("No space, selling all")
        pcall(function() SellAllItems:InvokeServer("Default", {}) end)
        processingItemsWon = false
        return false
    end
    
    for _, item in ipairs(sortedItems) do
        if keepCount < maxKeep then
            table.insert(keepGuids, item.guid)
            keepCount = keepCount + 1
            debugPrint("Keeping:", item.itemId, "Value:", item.price)
        else
            debugPrint("Selling:", item.itemId, "Value:", item.price)
        end
    end
    
    if #keepGuids > 0 then
        debugPrint("Keeping", #keepGuids, "items")
        pcall(function() SellAllItems:InvokeServer("Default", keepGuids) end)
    else
        debugPrint("No space, selling all")
        pcall(function() SellAllItems:InvokeServer("Default", {}) end)
    end
    
    processingItemsWon = false
    return true
end

local function bidOnGarage(garageName)
    debugPrint("Bidding on garage:", garageName)
    
    local bidStartTime = tick()
    local won = false
    
    while not won do
        if currentNextBid and currentBidder ~= LocalPlayer then
            debugPrint("Placing bid:", currentNextBid)
            pcall(function() PlaceBid:InvokeServer(currentNextBid) end)
        end
        
        if currentBidder == LocalPlayer then
            debugPrint("Won garage:", garageName)
            won = true
            break
        end
        
        if tick() - bidStartTime > 45 then
            debugPrint("Bid timeout on garage:", garageName)
            break
        end
        task.wait(BID_CHECK_INTERVAL)
    end
    
    return won
end

local function waitForGarage(garageNumber, timeout)
    local start = tick()
    while tick() - start < timeout do
        local garage = AuctionsFolder:FindFirstChild(tostring(garageNumber))
        if garage then
            debugPrint("Garage", garageNumber, "spawned")
            return garage
        end
        task.wait(0.1)
    end
    return nil
end

local function waitForGarageToEnd(garage, timeout)
    local start = tick()
    while tick() - start < timeout do
        if not garage.Parent then
            debugPrint("Garage", garage.Name, "ended")
            return true
        end
        task.wait(0.1)
    end
    return false
end

debugPrint("Auction bot started")

while true do
    while processingItemsWon do
        task.wait(0.25)
    end
    
    local bestAuction = findBestAffordableAuction()
    
    if not bestAuction then
        debugPrint("No affordable auction found, waiting 30 seconds")
        task.wait(30)
        continue
    end
    
    debugPrint("Best auction:", bestAuction.data.Name, "ID:", bestAuction.id)
    currentGarageId = bestAuction.id
    garagesWon = 0
    auctionComplete = false
    
    debugPrint("Entering queue for:", bestAuction.id)
    pcall(function() EnterQueue:InvokeServer(bestAuction.id) end)
    
    -- Bid on garage 1
    local garage1 = waitForGarage(1, 15)
    if garage1 then
        if bidOnGarage("1") then
            garagesWon = garagesWon + 1
        end
        waitForGarageToEnd(garage1, 60)
    end
    
    -- Bid on garage 2
    local garage2 = waitForGarage(2, 15)
    if garage2 then
        if bidOnGarage("2") then
            garagesWon = garagesWon + 1
        end
        waitForGarageToEnd(garage2, 60)
    end
    
    -- Bid on garage 3
    local garage3 = waitForGarage(3, 15)
    if garage3 then
        if bidOnGarage("3") then
            garagesWon = garagesWon + 1
        end
        waitForGarageToEnd(garage3, 60)
    end
    
    debugPrint("All garages done. Won", garagesWon, "out of 3")
    
    -- Wait for items to be awarded
    task.wait(3)
    
    -- Process items won
    handleItemsWon()
    
    debugPrint("Ready for next auction")
    task.wait(2)
end
