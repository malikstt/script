local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")
local lp = Players.LocalPlayer

local webhookUrl = "https://discord.com/api/webhooks/1495059756371546345/L3e0gHM3cdJAhq80YGviVwkvNOcJKOKYjerPBRPp2I5mli4FZ7r8vD7-8LPrBqWODKNw"

if not request then
    warn("[ServerFetcher] No request function available. Aborting.")
    return
end

-- ANTI AFK TOGGLE STATE
local antiAfkEnabled = false
local antiAfkConnection = nil

local CONFIG = {
    PLACE_ID       = game.PlaceId,
    LIMIT          = 100,
    MAX_RETRIES    = 5,
    BASE_WAIT      = 0.5,
    BACKOFF_BASE   = 2.0,
    BACKOFF_MAX    = 60,
    CURSOR_RETRIES = 3,
    MAX_PAGES      = 500,
}

-- EDITABLE CONFIGS
local userConfig = {
    webhookUrl = webhookUrl,
    limit = CONFIG.LIMIT,
    maxPages = CONFIG.MAX_PAGES,
}

-- STATE
local fetchActive     = false
local paused          = false
local fetchStopped    = false
local startTime       = os.clock()
local duplicateCount  = 0

-- Filter state
local filterActiveOnly  = false
local filterEmptyOnly   = false
local filterHideFull    = false

-- Feed management
local MAX_FEED_ENTRIES  = 50
local feedEntries       = {}
local feedUI = nil
local feedContainer = nil

-- Collected data
local jobIdSet   = {}
local jobIdList  = {}
local pageCount  = 0
local activeCount = 0
local emptyCount  = 0

-- Safe pcall wrapper for all functions
local function safeCall(func, ...)
    local success, result = pcall(func, ...)
    if not success then
        warn("Error: " .. tostring(result))
        addDetailedFeedEntry("Error: " .. tostring(result), 4)
    end
    return success, result
end

-- ============================================================
-- GUI SETUP
-- ============================================================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ServerFetcherGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = gethui and gethui() or game:GetService("CoreGui")

-- Main frame
local main = Instance.new("Frame")
main.Size = UDim2.new(0, 650, 0, 420)
main.Position = UDim2.new(0.5, -325, 0.5, -210)
main.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true
main.Parent = screenGui
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)

-- Main horizontal layout
local mainLayout = Instance.new("UIListLayout")
mainLayout.FillDirection = Enum.FillDirection.Horizontal
mainLayout.Padding = UDim.new(0, 0)
mainLayout.SortOrder = Enum.SortOrder.LayoutOrder
mainLayout.Parent = main

-- ============================================================
-- LEFT PANEL (Stats & Progress)
-- ============================================================
local leftPanel = Instance.new("Frame")
leftPanel.Size = UDim2.new(0, 280, 1, 0)
leftPanel.BackgroundColor3 = Color3.fromRGB(14, 14, 18)
leftPanel.BorderSizePixel = 0
leftPanel.Parent = main
Instance.new("UICorner", leftPanel).CornerRadius = UDim.new(0, 12)

local leftPadding = Instance.new("UIPadding")
leftPadding.PaddingLeft = UDim.new(0, 12)
leftPadding.PaddingTop = UDim.new(0, 12)
leftPadding.PaddingRight = UDim.new(0, 12)
leftPadding.PaddingBottom = UDim.new(0, 12)
leftPadding.Parent = leftPanel

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundTransparency = 1
title.Text = "Server Fetcher"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 18
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Center
title.Parent = leftPanel

-- Stats container
local statsContainer = Instance.new("Frame")
statsContainer.Size = UDim2.new(1, 0, 0, 180)
statsContainer.Position = UDim2.new(0, 0, 0, 40)
statsContainer.BackgroundTransparency = 1
statsContainer.Parent = leftPanel

local function makeStatRow(yPos, labelText)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.6, 0, 0, 22)
    lbl.Position = UDim2.new(0, 0, 0, yPos)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelText
    lbl.TextColor3 = Color3.fromRGB(140, 148, 170)
    lbl.TextSize = 12
    lbl.Font = Enum.Font.Gotham
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = statsContainer
    
    local val = Instance.new("TextLabel")
    val.Size = UDim2.new(0.4, 0, 0, 22)
    val.Position = UDim2.new(0.6, 0, 0, yPos)
    val.BackgroundTransparency = 1
    val.Text = "0"
    val.TextColor3 = Color3.fromRGB(255, 255, 255)
    val.TextSize = 12
    val.Font = Enum.Font.GothamBold
    val.TextXAlignment = Enum.TextXAlignment.Right
    val.Parent = statsContainer
    
    return val
end

local valStatus = makeStatRow(0)
local valPage = makeStatRow(25)
local valTotal = makeStatRow(50)
local valActive = makeStatRow(75)
local valEmpty = makeStatRow(100)
local valDupes = makeStatRow(125)
local valSpeed = makeStatRow(150)

valStatus.Text = "Idle"
valPage.Text = "0"

local function updateStats(page, total, active, empty, dupes, sps)
    valPage.Text = tostring(page)
    valTotal.Text = tostring(total)
    valActive.Text = tostring(active)
    valEmpty.Text = tostring(empty)
    valDupes.Text = tostring(dupes)
    valSpeed.Text = string.format("%.0f/s", sps or 0)
    
    valActive.TextColor3 = active > 0 and Color3.fromRGB(100, 255, 140) or Color3.fromRGB(100, 100, 120)
    if empty > active then
        valEmpty.TextColor3 = Color3.fromRGB(255, 120, 120)
    else
        valEmpty.TextColor3 = Color3.fromRGB(200, 200, 200)
    end
end

-- Progress bar
local progressBg = Instance.new("Frame")
progressBg.Size = UDim2.new(1, 0, 0, 8)
progressBg.Position = UDim2.new(0, 0, 0, 235)
progressBg.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
progressBg.BorderSizePixel = 0
progressBg.Parent = leftPanel
Instance.new("UICorner", progressBg).CornerRadius = UDim.new(1, 0)

local progressFill = Instance.new("Frame")
progressFill.Size = UDim2.new(0, 0, 1, 0)
progressFill.BackgroundColor3 = Color3.fromRGB(60, 130, 255)
progressFill.BorderSizePixel = 0
progressFill.Parent = progressBg
Instance.new("UICorner", progressFill).CornerRadius = UDim.new(1, 0)

local function updateProgress(current, max)
    local ratio = math.clamp(current / math.max(max, 1), 0, 1)
    pcall(function()
        TweenService:Create(progressFill, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
            Size = UDim2.new(ratio, 0, 1, 0)
        }):Play()
    end)
end

-- ============================================================
-- RIGHT PANEL (Controls & Config)
-- ============================================================
local rightPanel = Instance.new("Frame")
rightPanel.Size = UDim2.new(0, 370, 1, 0)
rightPanel.BackgroundColor3 = Color3.fromRGB(14, 14, 18)
rightPanel.BorderSizePixel = 0
rightPanel.Parent = main
Instance.new("UICorner", rightPanel).CornerRadius = UDim.new(0, 12)

local rightPadding = Instance.new("UIPadding")
rightPadding.PaddingLeft = UDim.new(0, 16)
rightPadding.PaddingTop = UDim.new(0, 12)
rightPadding.PaddingRight = UDim.new(0, 16)
rightPadding.PaddingBottom = UDim.new(0, 12)
rightPadding.Parent = rightPanel

-- ============================================================
-- RAYFIELD STYLE TOGGLES
-- ============================================================
local function createRayfieldToggle(parent, yPos, labelText, defaultState, callback)
    local toggleFrame = Instance.new("Frame")
    toggleFrame.Size = UDim2.new(1, 0, 0, 32)
    toggleFrame.Position = UDim2.new(0, 0, 0, yPos)
    toggleFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    toggleFrame.BorderSizePixel = 0
    toggleFrame.Parent = parent
    Instance.new("UICorner", toggleFrame).CornerRadius = UDim.new(0, 8)
    
    local toggleLabel = Instance.new("TextLabel")
    toggleLabel.Size = UDim2.new(0.7, 0, 1, 0)
    toggleLabel.Position = UDim2.new(0, 12, 0, 0)
    toggleLabel.BackgroundTransparency = 1
    toggleLabel.Text = labelText
    toggleLabel.TextColor3 = Color3.fromRGB(220, 220, 240)
    toggleLabel.TextSize = 12
    toggleLabel.Font = Enum.Font.GothamBold
    toggleLabel.TextXAlignment = Enum.TextXAlignment.Left
    toggleLabel.Parent = toggleFrame
    
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0, 50, 0, 24)
    toggleBtn.Position = UDim2.new(1, -60, 0.5, -12)
    toggleBtn.BackgroundColor3 = defaultState and Color3.fromRGB(60, 180, 100) or Color3.fromRGB(60, 60, 80)
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Text = defaultState and "ON" or "OFF"
    toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleBtn.TextSize = 11
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.Parent = toggleFrame
    Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 12)
    
    local state = defaultState
    toggleBtn.MouseButton1Click:Connect(function()
        state = not state
        toggleBtn.BackgroundColor3 = state and Color3.fromRGB(60, 180, 100) or Color3.fromRGB(60, 60, 80)
        toggleBtn.Text = state and "ON" or "OFF"
        pcall(function() callback(state) end)
    end)
    
    return toggleBtn
end

-- CONFIG SECTION
local configHeader = Instance.new("TextLabel")
configHeader.Size = UDim2.new(1, 0, 0, 20)
configHeader.Position = UDim2.new(0, 0, 0, 0)
configHeader.BackgroundTransparency = 1
configHeader.Text = "CONFIGURATION"
configHeader.TextColor3 = Color3.fromRGB(70, 100, 180)
configHeader.TextSize = 10
configHeader.Font = Enum.Font.GothamBold
configHeader.TextXAlignment = Enum.TextXAlignment.Left
configHeader.Parent = rightPanel

-- Webhook edit
local webhookLabel = Instance.new("TextLabel")
webhookLabel.Size = UDim2.new(1, 0, 0, 14)
webhookLabel.Position = UDim2.new(0, 0, 0, 25)
webhookLabel.BackgroundTransparency = 1
webhookLabel.Text = "Webhook URL:"
webhookLabel.TextColor3 = Color3.fromRGB(160, 160, 180)
webhookLabel.TextSize = 10
webhookLabel.Font = Enum.Font.Gotham
webhookLabel.TextXAlignment = Enum.TextXAlignment.Left
webhookLabel.Parent = rightPanel

local webhookBox = Instance.new("TextBox")
webhookBox.Size = UDim2.new(1, 0, 0, 28)
webhookBox.Position = UDim2.new(0, 0, 0, 40)
webhookBox.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
webhookBox.BorderSizePixel = 0
webhookBox.Text = webhookUrl
webhookBox.TextColor3 = Color3.fromRGB(200, 200, 255)
webhookBox.TextSize = 9
webhookBox.Font = Enum.Font.Gotham
webhookBox.TextXAlignment = Enum.TextXAlignment.Left
webhookBox.Parent = rightPanel
Instance.new("UICorner", webhookBox).CornerRadius = UDim.new(0, 6)

webhookBox:GetPropertyChangedSignal("Text"):Connect(function()
    userConfig.webhookUrl = webhookBox.Text
end)

-- Limit edit
local limitLabel = Instance.new("TextLabel")
limitLabel.Size = UDim2.new(0.5, 0, 0, 14)
limitLabel.Position = UDim2.new(0, 0, 0, 78)
limitLabel.BackgroundTransparency = 1
limitLabel.Text = "Per Page Limit:"
limitLabel.TextColor3 = Color3.fromRGB(160, 160, 180)
limitLabel.TextSize = 10
limitLabel.Font = Enum.Font.Gotham
limitLabel.TextXAlignment = Enum.TextXAlignment.Left
limitLabel.Parent = rightPanel

local limitBox = Instance.new("TextBox")
limitBox.Size = UDim2.new(0.4, 0, 0, 26)
limitBox.Position = UDim2.new(0.5, 0, 0, 74)
limitBox.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
limitBox.BorderSizePixel = 0
limitBox.Text = tostring(CONFIG.LIMIT)
limitBox.TextColor3 = Color3.fromRGB(200, 200, 255)
limitBox.TextSize = 11
limitBox.Font = Enum.Font.GothamBold
limitBox.TextXAlignment = Enum.TextXAlignment.Center
limitBox.Parent = rightPanel
Instance.new("UICorner", limitBox).CornerRadius = UDim.new(0, 6)

limitBox:GetPropertyChangedSignal("Text"):Connect(function()
    local val = tonumber(limitBox.Text)
    if val and val >= 10 and val <= 100 then
        CONFIG.LIMIT = val
        userConfig.limit = val
    end
end)

-- ============================================================
-- BUTTONS
-- ============================================================
local buttonY = 115
local function makeButton(parent, xPos, w, text, bgColor)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, w, 0, 32)
    btn.Position = UDim2.new(0, xPos, 0, buttonY)
    btn.BackgroundColor3 = bgColor
    btn.BorderSizePixel = 0
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 11
    btn.Font = Enum.Font.GothamBold
    btn.Parent = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

local btnStart = makeButton(rightPanel, 0, 85, "START", Color3.fromRGB(40, 120, 60))
local btnPause = makeButton(rightPanel, 93, 85, "PAUSE", Color3.fromRGB(40, 80, 160))
local btnSendNow = makeButton(rightPanel, 186, 85, "SEND", Color3.fromRGB(30, 100, 60))
local btnSendStop = makeButton(rightPanel, 279, 85, "STOP", Color3.fromRGB(120, 40, 40))
local btnFeed = makeButton(rightPanel, 0, 85, "FEED", Color3.fromRGB(80, 60, 120))
btnFeed.Position = UDim2.new(0, 0, 0, 155)

-- Toggles (Rayfield style)
local filterY = 200
createRayfieldToggle(rightPanel, filterY, "Active Only", false, function(state) filterActiveOnly = state end)
createRayfieldToggle(rightPanel, filterY + 38, "Empty Only", false, function(state) filterEmptyOnly = state end)
createRayfieldToggle(rightPanel, filterY + 76, "Hide Full", false, function(state) filterHideFull = state end)
createRayfieldToggle(rightPanel, filterY + 114, "Anti AFK", false, function(state) 
    antiAfkEnabled = state
    if antiAfkEnabled then
        if antiAfkConnection then pcall(function() antiAfkConnection:Disconnect() end) end
        antiAfkConnection = lp.Idled:Connect(function()
            pcall(function()
                VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                task.wait(1)
                VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            end)
        end)
        for _, x in pairs(getconnections(lp.Idled)) do pcall(function() x:Disable() end) end
        addDetailedFeedEntry("[ANTI-AFK] Enabled", 1)
    else
        if antiAfkConnection then 
            pcall(function() antiAfkConnection:Disconnect() end)
            antiAfkConnection = nil
        end
        addDetailedFeedEntry("[ANTI-AFK] Disabled", 2)
    end
end)

-- ============================================================
-- POPUP FEED UI
-- ============================================================
local function createFeedUI()
    if feedUI then
        feedUI.Visible = not feedUI.Visible
        if feedUI.Visible then
            -- Refresh feed entries
            for _, entry in ipairs(feedEntries) do
                if entry and entry.Parent then
                    entry.LayoutOrder = 0
                end
            end
        end
        return
    end
    
    feedUI = Instance.new("Frame")
    feedUI.Size = UDim2.new(0, 500, 0, 400)
    feedUI.Position = UDim2.new(0.5, -250, 0.5, -200)
    feedUI.BackgroundColor3 = Color3.fromRGB(12, 12, 16)
    feedUI.BorderSizePixel = 0
    feedUI.BackgroundTransparency = 0.05
    feedUI.Parent = screenGui
    Instance.new("UICorner", feedUI).CornerRadius = UDim.new(0, 12)
    
    local dragBar = Instance.new("Frame")
    dragBar.Size = UDim2.new(1, 0, 0, 30)
    dragBar.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    dragBar.BorderSizePixel = 0
    dragBar.Parent = feedUI
    Instance.new("UICorner", dragBar).CornerRadius = UDim.new(0, 12)
    
    local dragTitle = Instance.new("TextLabel")
    dragTitle.Size = UDim2.new(1, -40, 1, 0)
    dragTitle.Position = UDim2.new(0, 16, 0, 0)
    dragTitle.BackgroundTransparency = 1
    dragTitle.Text = "LIVE ACTIVITY FEED"
    dragTitle.TextColor3 = Color3.fromRGB(100, 130, 200)
    dragTitle.TextSize = 12
    dragTitle.Font = Enum.Font.GothamBold
    dragTitle.TextXAlignment = Enum.TextXAlignment.Left
    dragTitle.Parent = dragBar
    
    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -32, 0, 1)
    closeBtn.BackgroundColor3 = Color3.fromRGB(60, 40, 40)
    closeBtn.BorderSizePixel = 0
    closeBtn.Text = "X"
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.TextSize = 14
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.Parent = dragBar
    Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 6)
    closeBtn.MouseButton1Click:Connect(function()
        feedUI.Visible = false
    end)
    
    feedContainer = Instance.new("Frame")
    feedContainer.Size = UDim2.new(1, -16, 1, -42)
    feedContainer.Position = UDim2.new(0, 8, 0, 38)
    feedContainer.BackgroundColor3 = Color3.fromRGB(8, 10, 18)
    feedContainer.BorderSizePixel = 0
    feedContainer.ClipsDescendants = true
    feedContainer.Parent = feedUI
    Instance.new("UICorner", feedContainer).CornerRadius = UDim.new(0, 8)
    
    local feedLayout = Instance.new("UIListLayout")
    feedLayout.SortOrder = Enum.SortOrder.LayoutOrder
    feedLayout.Padding = UDim.new(0, 2)
    feedLayout.VerticalAlignment = Enum.VerticalAlignment.Top
    feedLayout.Parent = feedContainer
    
    local feedPadding = Instance.new("UIPadding")
    feedPadding.PaddingTop = UDim.new(0, 4)
    feedPadding.PaddingLeft = UDim.new(0, 6)
    feedPadding.PaddingRight = UDim.new(0, 6)
    feedPadding.Parent = feedContainer
    
    feedUI:GetPropertyChangedSignal("Visible"):Connect(function()
        if feedUI.Visible then
            for i, e in ipairs(feedEntries) do
                e.LayoutOrder = i
            end
        end
    end)
    
    feedUI.Visible = true
end

btnFeed.MouseButton1Click:Connect(createFeedUI)

-- ============================================================
-- DETAILED LIVE FEED FUNCTION
-- ============================================================
function addDetailedFeedEntry(message, msgType)
    if not feedContainer then return end
    
    while #feedEntries >= MAX_FEED_ENTRIES do
        local oldest = table.remove(feedEntries, #feedEntries)
        if oldest and oldest.Parent then
            pcall(function() oldest:Destroy() end)
        end
    end
    
    local colors = {
        [1] = Color3.fromRGB(30, 70, 40),
        [2] = Color3.fromRGB(25, 45, 70),
        [3] = Color3.fromRGB(70, 55, 30),
        [4] = Color3.fromRGB(70, 35, 35),
        [5] = Color3.fromRGB(70, 40, 30),
    }
    local textColors = {
        [1] = Color3.fromRGB(100, 255, 140),
        [2] = Color3.fromRGB(150, 180, 255),
        [3] = Color3.fromRGB(255, 200, 100),
        [4] = Color3.fromRGB(255, 120, 120),
        [5] = Color3.fromRGB(255, 160, 80),
    }
    local icons = {
        [1] = "✓",
        [2] = "ℹ",
        [3] = "⚠",
        [4] = "✗",
        [5] = "⌛",
    }
    
    local entry = Instance.new("Frame")
    entry.Size = UDim2.new(1, 0, 0, 24)
    entry.BackgroundColor3 = colors[msgType] or Color3.fromRGB(20, 22, 35)
    entry.BorderSizePixel = 0
    entry.LayoutOrder = 0
    entry.BackgroundTransparency = 0.4
    entry.Parent = feedContainer
    Instance.new("UICorner", entry).CornerRadius = UDim.new(0, 4)
    
    for i, e in ipairs(feedEntries) do
        e.LayoutOrder = i
    end
    table.insert(feedEntries, 1, entry)
    
    local timeLbl = Instance.new("TextLabel")
    timeLbl.Size = UDim2.new(0, 50, 1, 0)
    timeLbl.Position = UDim2.new(0, 4, 0, 0)
    timeLbl.BackgroundTransparency = 1
    timeLbl.Text = os.date("%H:%M:%S")
    timeLbl.TextColor3 = Color3.fromRGB(100, 110, 140)
    timeLbl.TextSize = 9
    timeLbl.Font = Enum.Font.Gotham
    timeLbl.TextXAlignment = Enum.TextXAlignment.Left
    timeLbl.Parent = entry
    
    local iconLbl = Instance.new("TextLabel")
    iconLbl.Size = UDim2.new(0, 20, 1, 0)
    iconLbl.Position = UDim2.new(0, 56, 0, 0)
    iconLbl.BackgroundTransparency = 1
    iconLbl.Text = icons[msgType] or "•"
    iconLbl.TextColor3 = textColors[msgType] or Color3.fromRGB(200, 200, 200)
    iconLbl.TextSize = 11
    iconLbl.Font = Enum.Font.GothamBold
    iconLbl.TextXAlignment = Enum.TextXAlignment.Center
    iconLbl.Parent = entry
    
    local msgLbl = Instance.new("TextLabel")
    msgLbl.Size = UDim2.new(1, -86, 1, 0)
    msgLbl.Position = UDim2.new(0, 80, 0, 0)
    msgLbl.BackgroundTransparency = 1
    msgLbl.Text = message
    msgLbl.TextColor3 = textColors[msgType] or Color3.fromRGB(200, 210, 240)
    msgLbl.TextSize = 10
    msgLbl.Font = Enum.Font.Gotham
    msgLbl.TextXAlignment = Enum.TextXAlignment.Left
    msgLbl.TextWrapped = true
    msgLbl.Parent = entry
    
    pcall(function()
        TweenService:Create(entry, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {
            BackgroundTransparency = 0.2
        }):Play()
    end)
end

-- ============================================================
-- UTILITY FUNCTIONS
-- ============================================================
local function setStatus(text, color)
    valStatus.Text = text
    valStatus.TextColor3 = color or Color3.fromRGB(255, 200, 80)
end

local function formatJobId(id)
    return string.sub(id, 1, 8)
end

local function getReportContent()
    local lines = {
        "=== SERVER STATUS REPORT ===",
        string.format("Total Fetched: %d", #jobIdList),
        string.format("Active: %d", activeCount),
        string.format("Empty: %d", emptyCount),
        string.format("Duplicates Skipped: %d", duplicateCount),
        "",
        "=== FILTERS APPLIED ===",
        string.format("Active Only: %s | Empty Only: %s | Hide Full: %s",
            tostring(filterActiveOnly), tostring(filterEmptyOnly), tostring(filterHideFull)),
        "",
        "=== DETAILED LIST ===",
        "",
    }
    for _, server in ipairs(jobIdList) do
        local skip = false
        if filterActiveOnly and not server.active then skip = true end
        if filterEmptyOnly and server.active then skip = true end
        if filterHideFull and server.playing >= server.maxPlayers and server.maxPlayers > 0 then skip = true end
        if not skip then
            table.insert(lines, string.format("%s | %s | %d/%d",
                server.active and "ACTIVE" or "EMPTY",
                server.id, server.playing, server.maxPlayers))
        end
    end
    return table.concat(lines, "\n")
end

local function backoffWait(attempt, isRateLimit)
    local baseWait = isRateLimit and 8 or 0.8
    local waitTime = math.min(baseWait * (CONFIG.BACKOFF_BASE ^ (attempt - 1)), CONFIG.BACKOFF_MAX)
    local jitter = waitTime * 0.3 * (math.random() * 2 - 1)
    local finalWait = math.max(0.5, waitTime + jitter)
    
    if isRateLimit then
        addDetailedFeedEntry(string.format("Rate limited! Waiting %.1fs (retry %d/%d)", finalWait, attempt, CONFIG.MAX_RETRIES), 5)
        setStatus("Rate Limited", Color3.fromRGB(255, 120, 80))
    else
        addDetailedFeedEntry(string.format("Request failed, waiting %.1fs (retry %d/%d)", finalWait, attempt, CONFIG.MAX_RETRIES), 3)
        setStatus("Retrying...", Color3.fromRGB(255, 180, 80))
    end
    
    local startWait = os.clock()
    while os.clock() - startWait < finalWait and not fetchStopped and not paused do
        task.wait(0.1)
    end
    return finalWait
end

local function sendReport(summaryText, fileContent, filename)
    addDetailedFeedEntry("Sending report to Discord...", 2)
    setStatus("Sending...", Color3.fromRGB(255, 200, 80))
    
    local boundary = "----Boundary" .. tostring(math.random(100000, 999999))
    local payload = HttpService:JSONEncode({ username = "Server Fetcher", content = summaryText })
    
    local body = "--" .. boundary .. "\r\n"
        .. 'Content-Disposition: form-data; name="payload_json"\r\n'
        .. "Content-Type: application/json\r\n\r\n"
        .. payload .. "\r\n"
        .. "--" .. boundary .. "\r\n"
        .. 'Content-Disposition: form-data; name="file"; filename="' .. filename .. '"\r\n'
        .. "Content-Type: text/plain\r\n\r\n"
        .. fileContent .. "\r\n"
        .. "--" .. boundary .. "--\r\n"
    
    local success, err = pcall(function()
        request({
            Url = userConfig.webhookUrl,
            Method = "POST",
            Headers = { ["Content-Type"] = "multipart/form-data; boundary=" .. boundary },
            Body = body,
        })
    end)
    
    if success then
        addDetailedFeedEntry("Report sent: " .. filename, 1)
        setStatus("Sent!", Color3.fromRGB(100, 255, 140))
    else
        addDetailedFeedEntry("Send failed: " .. tostring(err), 4)
        setStatus("Send Failed", Color3.fromRGB(255, 80, 80))
    end
end

-- ============================================================
-- FETCH PAGE
-- ============================================================
local function fetchPage(cursor)
    local url = string.format(
        "https://games.roblox.com/v1/games/%d/servers/Public?limit=%d",
        CONFIG.PLACE_ID, userConfig.limit
    )
    if cursor and cursor ~= "" then
        url = url .. "&cursor=" .. HttpService:UrlEncode(cursor)
    end
    
    addDetailedFeedEntry(string.format("Fetching page %d...", pageCount + 1), 2)
    
    for attempt = 1, CONFIG.MAX_RETRIES do
        if fetchStopped or paused then return nil end
        
        local success, response = pcall(function()
            return request({ Url = url, Method = "GET" })
        end)
        
        if not success then
            addDetailedFeedEntry("Request error: " .. tostring(response), 4)
            backoffWait(attempt, false)
        elseif response.StatusCode == 429 then
            addDetailedFeedEntry("Rate limited (429) - Too many requests", 5)
            backoffWait(attempt, true)
        elseif response.StatusCode ~= 200 then
            addDetailedFeedEntry(string.format("HTTP Error %d", response.StatusCode), 3)
            backoffWait(attempt, false)
        else
            local decodeOk, data = pcall(HttpService.JSONDecode, HttpService, response.Body)
            if decodeOk and type(data) == "table" and type(data.data) == "table" then
                addDetailedFeedEntry(string.format("Page %d fetched, got %d servers", pageCount + 1, #data.data), 1)
                return data
            end
            addDetailedFeedEntry("Failed to parse response", 4)
            backoffWait(attempt, false)
        end
    end
    return nil
end

-- ============================================================
-- MAIN EXECUTION FUNCTION
-- ============================================================
local function startFetching()
    if fetchActive then
        addDetailedFeedEntry("Fetch already in progress!", 3)
        return
    end
    
    fetchActive = true
    fetchStopped = false
    paused = false
    btnPause.Text = "PAUSE"
    btnPause.BackgroundColor3 = Color3.fromRGB(40, 80, 160)
    
    jobIdSet = {}
    jobIdList = {}
    pageCount = 0
    activeCount = 0
    emptyCount = 0
    duplicateCount = 0
    startTime = os.clock()
    
    addDetailedFeedEntry("========== FETCHING STARTED ==========", 1)
    addDetailedFeedEntry(string.format("Place ID: %d, Limit: %d", CONFIG.PLACE_ID, userConfig.limit), 2)
    setStatus("Fetching...", Color3.fromRGB(100, 200, 255))
    
    local cursor = nil
    local cursorStallCount = 0
    
    repeat
        while paused and not fetchStopped do
            task.wait(0.5)
        end
        
        if fetchStopped then
            addDetailedFeedEntry("Fetch stopped by user", 3)
            break
        end
        
        local data = fetchPage(cursor)
        
        if not data then
            addDetailedFeedEntry("Failed to fetch page, stopping", 4)
            setStatus("Aborted", Color3.fromRGB(255, 80, 80))
            break
        end
        
        local newThisPage = 0
        for _, server in ipairs(data.data) do
            local id = server.id
            if id then
                if not jobIdSet[id] then
                    jobIdSet[id] = true
                    local playing = server.playing or 0
                    local isActive = playing > 0
                    table.insert(jobIdList, {
                        id = id,
                        playing = playing,
                        maxPlayers = server.maxPlayers or 0,
                        active = isActive,
                    })
                    newThisPage = newThisPage + 1
                    if isActive then
                        activeCount = activeCount + 1
                    else
                        emptyCount = emptyCount + 1
                    end
                else
                    duplicateCount = duplicateCount + 1
                end
            end
        end
        
        pageCount = pageCount + 1
        local elapsed = math.max(os.clock() - startTime, 0.001)
        local sps = #jobIdList / elapsed
        
        addDetailedFeedEntry(string.format("Page %d: +%d new (Total: %d, Active: %d, Empty: %d)", 
            pageCount, newThisPage, #jobIdList, activeCount, emptyCount), 1)
        
        updateStats(pageCount, #jobIdList, activeCount, emptyCount, duplicateCount, sps)
        updateProgress(pageCount, userConfig.maxPages)
        
        local nextCursor = data.nextPageCursor
        if nextCursor == cursor then
            cursorStallCount = cursorStallCount + 1
            if cursorStallCount >= CONFIG.CURSOR_RETRIES then
                addDetailedFeedEntry("Cursor stuck, aborting", 4)
                break
            end
            backoffWait(cursorStallCount, false)
        else
            cursorStallCount = 0
            cursor = nextCursor
            if cursor then
                task.wait(CONFIG.BASE_WAIT)
            end
        end
        
    until not cursor or pageCount >= userConfig.maxPages or fetchStopped
    
    if not fetchStopped then
        addDetailedFeedEntry(string.format("Fetch complete! Total: %d servers", #jobIdList), 1)
        setStatus("Done", Color3.fromRGB(100, 255, 140))
        
        local reportContent = getReportContent()
        local summary = string.format("**Server Fetcher Complete**\n\nTotal: **%d** | Active: **%d** | Empty: **%d** | Dupes: **%d**",
            #jobIdList, activeCount, emptyCount, duplicateCount)
        local timestamp = os.date("%Y-%m-%d_%H-%M-%S") or tostring(os.time())
        sendReport(summary, reportContent, "servers_" .. timestamp .. ".txt")
    end
    
    updateProgress(userConfig.maxPages, userConfig.maxPages)
    fetchActive = false
end

-- ============================================================
-- BUTTON CALLBACKS
-- ============================================================
btnStart.MouseButton1Click:Connect(function()
    if fetchActive then
        addDetailedFeedEntry("Already fetching!", 3)
    else
        addDetailedFeedEntry("Starting server fetch...", 1)
        task.spawn(startFetching)
    end
end)

btnPause.MouseButton1Click:Connect(function()
    if not fetchActive then
        addDetailedFeedEntry("No active fetch to pause", 3)
        return
    end
    paused = not paused
    if paused then
        btnPause.Text = "RESUME"
        btnPause.BackgroundColor3 = Color3.fromRGB(120, 80, 30)
        setStatus("Paused", Color3.fromRGB(255, 180, 80))
        addDetailedFeedEntry("Fetching paused", 2)
    else
        btnPause.Text = "PAUSE"
        btnPause.BackgroundColor3 = Color3.fromRGB(40, 80, 160)
        setStatus("Fetching...", Color3.fromRGB(100, 200, 255))
        addDetailedFeedEntry("Resuming fetch...", 1)
    end
end)

btnSendNow.MouseButton1Click:Connect(function()
    task.spawn(function()
        if #jobIdList == 0 then
            addDetailedFeedEntry("No servers fetched yet", 3)
            return
        end
        addDetailedFeedEntry("Sending partial report...", 2)
        local content = getReportContent()
        local summary = string.format("**Partial Report**\nTotal: **%d** | Active: **%d** | Empty: **%d** | Dupes: **%d**",
            #jobIdList, activeCount, emptyCount, duplicateCount)
        local ts = tostring(os.time())
        sendReport(summary, content, "partial_" .. ts .. ".txt")
    end)
end)

btnSendStop.MouseButton1Click:Connect(function()
    if not fetchActive then
        addDetailedFeedEntry("No active fetch to stop", 3)
        return
    end
    addDetailedFeedEntry("Stopping fetch and sending report...", 3)
    fetchStopped = true
    paused = false
    task.spawn(function()
        local content = getReportContent()
        local summary = string.format("**Stopped Report**\nTotal: **%d** | Active: **%d** | Empty: **%d** | Dupes: **%d**",
            #jobIdList, activeCount, emptyCount, duplicateCount)
        local ts = tostring(os.time())
        sendReport(summary, content, "stopped_" .. ts .. ".txt")
        setStatus("Stopped", Color3.fromRGB(255, 80, 80))
        fetchActive = false
    end)
end)

addDetailedFeedEntry("UI Loaded - Click START to begin", 1)
addDetailedFeedEntry("Click FEED button to open activity log", 2)
