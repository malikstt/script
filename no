local HttpService = game:GetService("HttpService")

local webhookUrl = "https://discord.com/api/webhooks/1495059756371546345/L3e0gHM3cdJAhq80YGviVwkvNOcJKOKYjerPBRPp2I5mli4FZ7r8vD7-8LPrBqWODKNw"

local request = request or http_request or (syn and syn.request)
if not request then
    warn("[ServerFetcher] No HTTP request function available. Aborting.")
    return
end

local CONFIG = {
    PLACE_ID       = game.PlaceId,
    LIMIT          = 100,
    MAX_RETRIES    = 5,
    BASE_WAIT      = 0.5,
    BACKOFF_BASE   = 2.0,
    BACKOFF_MAX    = 60,
    CURSOR_RETRIES = 3,
    MAX_PAGES      = 500,
}

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ServerFetcherGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = gethui and gethui() or game:GetService("CoreGui")

local main = Instance.new("Frame")
main.Size = UDim2.new(0, 340, 0, 280)
main.Position = UDim2.new(0, 20, 0.5, -140)
main.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true
main.Parent = screenGui

Instance.new("UICorner", main).CornerRadius = UDim.new(0, 8)

local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 36)
titleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
titleBar.BorderSizePixel = 0
titleBar.Parent = main
Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 8)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -10, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.BackgroundTransparency = 1
title.Text = "🌐 Server Fetcher"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 15
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = titleBar

local function makeLabel(yPos, labelText, defaultVal, color)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, -20, 0, 28)
    container.Position = UDim2.new(0, 10, 0, yPos)
    container.BackgroundTransparency = 1
    container.Parent = main

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.55, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelText
    lbl.TextColor3 = Color3.fromRGB(160, 160, 160)
    lbl.TextSize = 13
    lbl.Font = Enum.Font.Gotham
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = container

    local val = Instance.new("TextLabel")
    val.Size = UDim2.new(0.45, 0, 1, 0)
    val.Position = UDim2.new(0.55, 0, 0, 0)
    val.BackgroundTransparency = 1
    val.Text = defaultVal
    val.TextColor3 = color or Color3.fromRGB(255, 255, 255)
    val.TextSize = 13
    val.Font = Enum.Font.GothamBold
    val.TextXAlignment = Enum.TextXAlignment.Right
    val.Parent = container

    return val
end

local valStatus   = makeLabel(46,  "Status:",          "Idle",   Color3.fromRGB(255, 200, 50))
local valPage     = makeLabel(78,  "Pages fetched:",   "0",      Color3.fromRGB(255, 255, 255))
local valTotal    = makeLabel(110, "Unique servers:",  "0",      Color3.fromRGB(100, 200, 255))
local valActive   = makeLabel(142, "Active servers:",  "0",      Color3.fromRGB(100, 255, 140))
local valEmpty    = makeLabel(174, "Empty servers:",   "0",      Color3.fromRGB(200, 200, 200))
local valNew      = makeLabel(206, "New this page:",   "—",      Color3.fromRGB(255, 180, 80))

local div = Instance.new("Frame")
div.Size = UDim2.new(1, -20, 0, 1)
div.Position = UDim2.new(0, 10, 0, 238)
div.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
div.BorderSizePixel = 0
div.Parent = main

local bottomLabel = Instance.new("TextLabel")
bottomLabel.Size = UDim2.new(1, -20, 0, 32)
bottomLabel.Position = UDim2.new(0, 10, 0, 242)
bottomLabel.BackgroundTransparency = 1
bottomLabel.Text = "Waiting to start..."
bottomLabel.TextColor3 = Color3.fromRGB(120, 120, 120)
bottomLabel.TextSize = 12
bottomLabel.Font = Enum.Font.Gotham
bottomLabel.TextXAlignment = Enum.TextXAlignment.Left
bottomLabel.TextWrapped = true
bottomLabel.Parent = main

local function setStatus(text, color)
    valStatus.Text = text
    valStatus.TextColor3 = color or Color3.fromRGB(255, 200, 50)
end

local function setBottom(text)
    bottomLabel.Text = text
end

local function updateStats(page, total, active, empty, newPage)
    valPage.Text   = tostring(page)
    valTotal.Text  = tostring(total)
    valActive.Text = tostring(active)
    valEmpty.Text  = tostring(empty)
    valNew.Text    = newPage ~= nil and tostring(newPage) or "—"
end

local function log(tag, msg)
    print(string.format("[ServerFetcher][%s] %s", tag, msg))
end

local function backoffWait(attempt, isRateLimit)
    local base = isRateLimit and 8 or 0.8
    local wait = math.min(base * (CONFIG.BACKOFF_BASE ^ (attempt - 1)), CONFIG.BACKOFF_MAX)
    local jitter = wait * 0.3 * (math.random() * 2 - 1)
    local final = math.max(0.5, wait + jitter)
    setBottom(string.format("⏳ Waiting %.1fs (attempt %d)", final, attempt))
    task.wait(final)
end

local function fetchPage(cursor)
    local url = string.format(
        "https://games.roblox.com/v1/games/%d/servers/Public?limit=%d",
        CONFIG.PLACE_ID, CONFIG.LIMIT
    )
    if cursor and cursor ~= "" then
        url = url .. "&cursor=" .. HttpService:UrlEncode(cursor)
    end

    for attempt = 1, CONFIG.MAX_RETRIES do
        setBottom(string.format("📡 Fetching page %d (attempt %d)...", valPage.Text + 1, attempt))
        local ok, response = pcall(request, { Url = url, Method = "GET" })

        if not ok then
            log("ERROR", "request() threw: " .. tostring(response))
            backoffWait(attempt, false)
        elseif response.StatusCode == 429 then
            log("RATE_LIMIT", "429 on attempt " .. attempt)
            setStatus("Rate Limited", Color3.fromRGB(255, 80, 80))
            backoffWait(attempt, true)
        elseif response.StatusCode ~= 200 then
            log("WARN", "HTTP " .. response.StatusCode)
            backoffWait(attempt, false)
        else
            local decodeOk, data = pcall(HttpService.JSONDecode, HttpService, response.Body)
            if decodeOk and type(data) == "table" and type(data.data) == "table" then
                return data
            end
            backoffWait(attempt, false)
        end
    end
    return nil
end

local function sendReport(summaryText, fileContent, filename)
    setStatus("Sending...", Color3.fromRGB(255, 200, 50))
    setBottom("📤 Sending report to Discord...")

    local boundary = "----Boundary" .. tostring(math.random(100000, 999999))
    local payload = HttpService:JSONEncode({ username = "Server Fetcher", content = summaryText })

    local body = "--" .. boundary .. "\r\n"
        .. 'Content-Disposition: form-data; name="payload_json"\r\n'
        .. "Content-Type: application/json\r\n\r\n"
        .. payload .. "\r\n"
        .. "--" .. boundary .. "\r\n"
        .. 'Content-Disposition: form-data; name="file"; filename="' .. filename .. '"\r\n"
        .. "Content-Type: text/plain\r\n\r\n"
        .. fileContent .. "\r\n"
        .. "--" .. boundary .. "--\r\n"

    local ok, err = pcall(function()
        request({
            Url     = webhookUrl,
            Method  = "POST",
            Headers = { ["Content-Type"] = "multipart/form-data; boundary=" .. boundary },
            Body    = body,
        })
    end)

    if ok then
        log("WEBHOOK", "Sent: " .. filename)
        setBottom("✅ Report sent to Discord!")
    else
        log("ERROR", "Webhook failed: " .. tostring(err))
        setBottom("❌ Webhook failed")
    end
end

task.spawn(function()
    setStatus("Fetching...", Color3.fromRGB(255, 200, 50))
    log("START", string.format("PlaceId %d", CONFIG.PLACE_ID))

    local jobIdSet = {}
    local jobIdList = {}
    local pageCount = 0
    local activeCount = 0
    local emptyCount = 0

    local cursor = nil
    local lastCursor = nil
    local cursorStallCount = 0

    repeat
        local data = fetchPage(cursor)

        if not data then
            log("ABORT", "fetchPage nil. Stopping.")
            setStatus("Aborted", Color3.fromRGB(255, 80, 80))
            break
        end

        local newThisPage = 0
        for _, server in ipairs(data.data) do
            local id = server.id
            if id and not jobIdSet[id] then
                jobIdSet[id] = true
                local playing = server.playing or 0
                local isActive = playing > 0
                table.insert(jobIdList, {
                    id = id,
                    playing = playing,
                    maxPlayers = server.maxPlayers or 0,
                    active = isActive,
                })
                newThisPage = newThisPage + 1
                if isActive then
                    activeCount = activeCount + 1
                else
                    emptyCount = emptyCount + 1
                end
            end
        end

        pageCount = pageCount + 1
        
        log("PAGE", string.format("#%d | new=%d | total=%d", pageCount, newThisPage, #jobIdList))
        setStatus(string.format("Page %d", pageCount), Color3.fromRGB(100, 200, 255))
        updateStats(pageCount, #jobIdList, activeCount, emptyCount, newThisPage)
        setBottom(string.format("✅ Fetched %d servers so far", #jobIdList))

        local nextCursor = nil
        if data.nextPageCursor and data.nextPageCursor ~= "" then
            nextCursor = data.nextPageCursor
        end

        if nextCursor == cursor then
            cursorStallCount = cursorStallCount + 1
            if cursorStallCount >= CONFIG.CURSOR_RETRIES then
                log("ABORT", "Cursor stuck")
                setStatus("Aborted", Color3.fromRGB(255, 80, 80))
                break
            end
            backoffWait(cursorStallCount, false)
        else
            cursorStallCount = 0
        end

        lastCursor = cursor
        cursor = nextCursor

        if cursor then
            task.wait(CONFIG.BASE_WAIT)
        end

    until not cursor or pageCount >= CONFIG.MAX_PAGES

    setStatus("Building report...", Color3.fromRGB(255, 200, 50))

    local reportLines = {
        "=== SERVER STATUS REPORT ===",
        string.format("Total Fetched: %d", #jobIdList),
        string.format("Active (playing > 0): %d", activeCount),
        string.format("Empty (0 players): %d", emptyCount),
        "",
        "=== DETAILED LIST ===",
        "",
    }

    for _, server in ipairs(jobIdList) do
        table.insert(reportLines, string.format("%s | %s | %d/%d players",
            server.active and "ACTIVE" or "EMPTY",
            server.id, server.playing, server.maxPlayers))
    end

    local reportContent = table.concat(reportLines, "\n")
    local summary = string.format("**Server Fetcher Complete**\n\nTotal: **%d** | Active: **%d** | Empty: **%d**\n📁 Full list attached.",
        #jobIdList, activeCount, emptyCount)

    local timestamp = os.date("%Y-%m-%d_%H-%M-%S")
    if timestamp == "" then timestamp = tostring(os.time()) end

    sendReport(summary, reportContent, "servers_" .. timestamp .. ".txt")

    setStatus("✅ Done", Color3.fromRGB(100, 255, 140))
    setBottom(string.format("Complete! %d servers fetched", #jobIdList))
    log("DONE", string.format("Pages=%d | Unique=%d | Active=%d | Empty=%d",
        pageCount, #jobIdList, activeCount, emptyCount))
end)
