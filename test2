local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")

local Paper = require(ReplicatedStorage:WaitForChild("Paper"))
local Stats = Paper.Stats
local Network = Paper.Network
local UniverseModule = require(ReplicatedStorage:WaitForChild("Modules").Shared.Universe)
local RebirthModule = require(ReplicatedStorage:WaitForChild("Modules").Shared.Rebirths)

repeat task.wait() until Stats.IsLoaded(LocalPlayer)

local Remotes = ReplicatedStorage:WaitForChild("Paper"):WaitForChild("Remotes")
local RemoteFunction = Remotes:WaitForChild("__remotefunction")

local AutoTrain = Stats.Get("AutoTrain")
local AutoThrow = Stats.Get("AutoThrow")
local CurrentUniverse = Stats.Get("CurrentUniverse")

local RebirthsEaster = require(ReplicatedStorage:WaitForChild("Tables").RebirthsEaster)
local RebirthsUni1 = require(ReplicatedStorage:WaitForChild("Tables").Rebirths)
local RebirthsUni2 = require(ReplicatedStorage:WaitForChild("Tables").RebirthsUni2)

local function GetRebirthTable()
    local universe = CurrentUniverse and CurrentUniverse.Value or 0
    if universe == 0 then
        return RebirthsEaster
    elseif universe == 1 then
        return RebirthsUni1
    elseif universe == 2 then
        return RebirthsUni2
    end
    return RebirthsEaster
end

local function GetCurrentMoney()
    local universe = CurrentUniverse and CurrentUniverse.Value or 0
    return UniverseModule.GetMoneyStat(LocalPlayer, universe)
end

local function GetRebirthSkips()
    local universe = CurrentUniverse and CurrentUniverse.Value or 0
    return UniverseModule.GetRebirthSkipsStat(LocalPlayer, universe)
end

local function GetRebirthCost(rebirthAmount)
    local universe = CurrentUniverse and CurrentUniverse.Value or 0
    return RebirthModule.GetRebirthCost(LocalPlayer, rebirthAmount, universe)
end

local function GetMaxUnlockedRebirthIndex()
    local skipsStat = GetRebirthSkips()
    local skips = skipsStat and skipsStat.Value or 0
    local universe = CurrentUniverse and CurrentUniverse.Value or 0
    local extra = (universe == 2 or universe == 0) and 1 or 8
    return skips + extra
end

local function GetBestAffordableRebirth()
    local moneyStat = GetCurrentMoney()
    local currentMoney = moneyStat and moneyStat.Value or 0
    local rebirthTable = GetRebirthTable()
    local maxUnlocked = GetMaxUnlockedRebirthIndex()
    local bestAmount = 0
    local bestIndex = 0
    
    for index, amount in ipairs(rebirthTable) do
        if index <= maxUnlocked then
            local cost = GetRebirthCost(amount)
            if cost <= currentMoney and amount > bestAmount then
                bestAmount = amount
                bestIndex = index
            end
        end
    end
    
    return bestAmount, bestIndex
end

local function PerformRebirth(rebirthIndex)
    pcall(function()
        local args = { "Rebirth", rebirthIndex }
        RemoteFunction:InvokeServer(unpack(args))
        print("[Rebirth] Performed rebirth with index:", rebirthIndex)
    end)
end

local function AutoRebirthBest()
    local bestAmount, bestIndex = GetBestAffordableRebirth()
    if bestIndex > 0 then
        print(string.format("[Rebirth] Best affordable: %s (index %s)", bestAmount, bestIndex))
        PerformRebirth(bestIndex)
        return true
    else
        print("[Rebirth] Cannot afford any unlocked rebirth")
        return false
    end
end

local function DisableAutoTrain()
    if AutoTrain and AutoTrain.Value then
        Network.FireServer("Toggle Setting", "AutoTrain")
        print("[AutoTrain] Disabled")
    end
    
    for i = 1, 5 do
        pcall(function()
            local character = LocalPlayer.Character
            if character then
                local humanoid = character:FindFirstChild("Humanoid")
                if humanoid then
                    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
            task.wait(0.05)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
        end)
        task.wait(0.1)
    end
    
    LocalPlayer:SetAttribute("TrainingStation", nil)
    task.wait(0.5)
end

local function EnableAutoThrow()
    if AutoThrow and not AutoThrow.Value then
        Network.FireServer("Toggle Setting", "AutoThrow")
        print("[AutoThrow] Enabled")
    else
        print("[AutoThrow] Already enabled")
    end
end

local function TrainOnly()
    print("[Cycle] Starting training phase (60 seconds)...")
    
    if AutoTrain and not AutoTrain.Value then
        Network.FireServer("Toggle Setting", "AutoTrain")
        print("[AutoTrain] Enabled")
    end
    
    for i = 60, 1, -1 do
        if i % 10 == 0 then
            print("[Train] " .. i .. " seconds remaining")
        end
        task.wait(1)
    end
    
    print("[Cycle] Training phase complete")
end

local function ThrowOnly()
    print("[Cycle] Starting throwing phase (90 seconds)...")
    
    DisableAutoTrain()
    EnableAutoThrow()
    
    for i = 90, 1, -1 do
        if i % 15 == 0 then
            print("[Throw] " .. i .. " seconds remaining")
        end
        task.wait(1)
    end
    
    print("[Cycle] Throwing phase complete")
end

local actions = {
    { args = {"Upgrade", "More Rebirth Skips"} },
    { args = {"Unlock Next World"} },
    { args = {"Buy Next Object"} }
}

local lastActionIndex = nil

local function fireRandomAction()
    local availableIndices = {}
    
    for i = 1, #actions do
        if i ~= lastActionIndex then
            table.insert(availableIndices, i)
        end
    end
    
    local randomIndex = availableIndices[math.random(1, #availableIndices)]
    local selectedAction = actions[randomIndex]
    
    pcall(function()
        RemoteFunction:InvokeServer(unpack(selectedAction.args))
        print("[Action] Fired:", unpack(selectedAction.args))
    end)
    
    lastActionIndex = randomIndex
end

task.spawn(function()
    while true do
        task.wait(10)
        pcall(AutoRebirthBest)
    end
end)

task.spawn(function()
    fireRandomAction()
    while true do
        task.wait(5)
        fireRandomAction()
    end
end)

print("[Farm] Starting loop: Train 60s -> Throw 90s")

while true do
    TrainOnly()
    ThrowOnly()
end
