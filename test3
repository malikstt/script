local TextChatService = game:GetService("TextChatService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local webhookUrl = "https://discord.com/api/webhooks/1492622522339037315/wC2sJfo1IepeHxG4sLAb9226eLBu91EKFmjb6TFi5CBXaSsbSRRUGp84MpZIzOFsIAZd"
local pingUserId = "198241023866961920"  -- User to ping: shakeem_hood

-- Allowed pets list (for contains detection)
local allowedPets = {
	["Horror Book"] = true,
	["Cursed Hell Eye"] = true,
	["Horror Monster"] = true,
	["Enchanted Inferno"] = true
}

print("Started - Monitoring for pet names in chat")

local function stripFontTags(str)
	return str:gsub("<.->","")
end

local function extractPlayerName(text)
	local atIndex = string.find(text, "@")
	if not atIndex then return nil end
	local startPos = atIndex + 1
	local endPos = string.find(text, " ", startPos)
	if not endPos then return nil end
	return string.sub(text, startPos, endPos - 1)
end

local function findPetData(petName)
	local petsFolder = ReplicatedStorage:FindFirstChild("Pets")
	if not petsFolder then 
		print("Pets folder not found in ReplicatedStorage")
		return nil 
	end
	
	for _, petModule in pairs(petsFolder:GetChildren()) do
		local success, petInfo = pcall(require, petModule)
		if success and petInfo.PetName == petName then
			return petInfo
		end
	end
	print("Could not find pet data for:", petName)
	return nil
end

local function getAssetImageUrl(assetId)
	if not assetId then return nil end
	assetId = tostring(assetId):match("%d+")
	local url = "https://thumbnails.roblox.com/v1/assets?assetIds="..assetId.."&size=420x420&format=Png&isCircular=false"
	
	local success, response = pcall(function()
		return request({
			Url = url,
			Method = "GET"
		})
	end)
	
	if success and response.Success then
		local data = HttpService:JSONDecode(response.Body)
		if data and data.data and data.data[1] and data.data[1].imageUrl then
			return data.data[1].imageUrl
		end
	end
	return nil
end

local function getPlayerAvatarUrl(userId)
	local url = "https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds="..userId.."&size=420x420&format=Png&isCircular=true"
	
	local success, response = pcall(function()
		return request({
			Url = url,
			Method = "GET"
		})
	end)
	
	if success and response.Success then
		local data = HttpService:JSONDecode(response.Body)
		if data and data.data and data.data[1] and data.data[1].imageUrl then
			return data.data[1].imageUrl
		end
	end
	return nil
end

local function getVariantDisplay(variant)
	if variant == "Rainbow" then
		return "🌈 Rainbow"
	elseif variant == "Golden" then
		return "⭐ Golden"
	else
		return "None (Normal)"
	end
end

local function sendWebhook(player, petName, variant, petInfo)
	local imageUrl = nil
	local avatarUrl = nil
	
	-- Try to get pet image, use placeholder if fails
	if petInfo then
		if variant == "Golden" and petInfo.IconGold then
			imageUrl = getAssetImageUrl(petInfo.IconGold)
		else
			imageUrl = getAssetImageUrl(petInfo.Icon or petInfo.AssetId)
		end
	end
	
	avatarUrl = getPlayerAvatarUrl(player.UserId)
	
	-- Keep trying for avatar (required)
	while not avatarUrl do
		task.wait(0.5)
		avatarUrl = getPlayerAvatarUrl(player.UserId)
	end
	
	-- For pet image, don't wait forever - send without it if fails
	local attemptCount = 0
	while not imageUrl and attemptCount < 10 do
		task.wait(0.5)
		attemptCount = attemptCount + 1
		if petInfo then
			if variant == "Golden" and petInfo.IconGold then
				imageUrl = getAssetImageUrl(petInfo.IconGold)
			else
				imageUrl = getAssetImageUrl(petInfo.Icon or petInfo.AssetId)
			end
		end
	end
	
	-- Use fallback values if petInfo is missing
	local displayPetName = petName
	local rarityText = "Unknown"
	local boostText = "Unknown"
	local chanceText = "Unknown"
	local powerValue = 0
	local boostValue = 0
	
	if petInfo then
		displayPetName = petInfo.PetName or petName
		rarityText = petInfo.Rarity or "Unknown"
		boostValue = petInfo.Boost or 0
		powerValue = petInfo.Power or 0
		chanceText = petInfo.Chance or "Unknown"
		boostText = string.format("+%d%% / %s", boostValue, string.format("%.0f", powerValue))
	else
		boostText = "Unknown"
	end
	
	local pingText = string.format("<@%s>", pingUserId)
	
	-- Use default image if no pet image found
	local finalImageUrl = imageUrl or "https://i.imgur.com/placeholder.png"
	
	local payload = {
		username = "Exclusive Hatch",
		content = pingText,
		embeds = {{
			description = string.format("Hatched **%s** %s!", displayPetName, chanceText),
			color = 0xFFD700,
			thumbnail = {
				url = finalImageUrl
			},
			author = {
				name = player.Name,
				icon_url = avatarUrl
			},
			fields = {
				{
					name = "Rarity",
					value = rarityText,
					inline = true
				},
				{
					name = "Variant",
					value = getVariantDisplay(variant),
					inline = true
				},
				{
					name = "Stats",
					value = boostText,
					inline = true
				}
			}
		}}
	}
	
	local success, err = pcall(function()
		HttpService:RequestAsync({
			Url = webhookUrl,
			Method = "POST",
			Headers = {
				["Content-Type"] = "application/json"
			},
			Body = HttpService:JSONEncode(payload)
		})
	end)
	
	if success then
		print(string.format("Sent webhook for %s - %s (%s)", player.Name, displayPetName, variant))
	else
		warn("Failed to send webhook:", err)
	end
end

-- Main chat listener
TextChatService.OnIncomingMessage = function(message)
	if not message.Text then return end
	
	pcall(function()
		local text = stripFontTags(message.Text)
		local loweredText = string.lower(text)
		
		-- Extract player name
		local playerName = extractPlayerName(text)
		if not playerName then return end
		
		-- CONTAINS DETECTION: Check if any allowed pet is in the message
		local detectedPet = nil
		for petName in pairs(allowedPets) do
			if string.find(loweredText, string.lower(petName), 1, true) then
				detectedPet = petName
				break
			end
		end
		
		if not detectedPet then
			return  -- Silent ignore
		end
		
		-- VARIANT DETECTION: Simple contains check for rainbow/golden
		local variant = "Normal"
		if string.find(loweredText, "rainbow") then
			variant = "Rainbow"
		elseif string.find(loweredText, "golden") then
			variant = "Golden"
		end
		
		print(string.format("Detected: %s said %s (%s)", playerName, detectedPet, variant))
		
		-- Try to get pet data, but continue even if fails
		local petInfo = findPetData(detectedPet)
		if not petInfo then
			print("WARNING: No pet data found for " .. detectedPet .. ", sending with Unknown values")
		end
		
		-- Get player object
		local player = Players:FindFirstChild(playerName)
		if not player then 
			print("Player not found:", playerName)
			return 
		end
		
		-- Send webhook (works even without petInfo)
		sendWebhook(player, detectedPet, variant, petInfo)
	end)
end