local TextChatService = game:GetService("TextChatService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local webhookUrl = "https://discord.com/api/webhooks/1492622522339037315/wC2sJfo1IepeHxG4sLAb9226eLBu91EKFmjb6TFi5CBXaSsbSRRUGp84MpZIzOFsIAZd"
local pingUserId = "198241023866961920"

local petChances = {
	["Horror Book"] = "1 in 50",
	["Cursed Hell Eye"] = "1 in 100",
	["Horror Monster"] = "1 in 1,000",
	["Enchanted Inferno"] = "1 in 1,000"
}

local allowedPets = {
	["Horror Book"] = true,
	["Cursed Hell Eye"] = true,
	["Horror Monster"] = true,
	["Enchanted Inferno"] = true
}

local sentMessages = {}

print("Started - Monitoring for pet names in chat fr")

local function stripFontTags(str)
	return str:gsub("<.->","")
end

local function extractPlayerName(text)
	local atIndex = string.find(text, "@")
	if not atIndex then return nil end
	local startPos = atIndex + 1
	local endPos = string.find(text, " ", startPos)
	if not endPos then return nil end
	return string.sub(text, startPos, endPos - 1)
end

local function findPetData(petName)
	local petsFolder = ReplicatedStorage:FindFirstChild("Pets")
	if not petsFolder then 
		print("Pets folder not found in ReplicatedStorage")
		return nil 
	end
	
	for _, petModule in pairs(petsFolder:GetChildren()) do
		local success, petInfo = pcall(require, petModule)
		if success and petInfo.PetName == petName then
			return petInfo
		end
	end
	print("Could not find pet data for:", petName)
	return nil
end

local function extractAssetId(str)
	if not str or str == "" then return nil end
	return tostring(str):match("%d+")
end

local function getAssetImageUrl(assetId)
	if not assetId then return nil end
	
	local url = "https://thumbnails.roblox.com/v1/assets?assetIds="..assetId.."&size=420x420&format=Png&isCircular=false"
	
	local success, response = pcall(function()
		return request({
			Url = url,
			Method = "GET"
		})
	end)
	
	if success and response and response.Success then
		local data = HttpService:JSONDecode(response.Body)
		if data and data.data and data.data[1] and data.data[1].imageUrl then
			return data.data[1].imageUrl
		end
	end
	
	return nil
end

local function getPetImageUrl(petInfo, variant)
	if not petInfo then return nil end
	
	local assetId = nil
	
	if variant == "Golden" and petInfo.IconGold then
		assetId = extractAssetId(petInfo.IconGold)
	elseif petInfo.Icon then
		assetId = extractAssetId(petInfo.Icon)
	end
	
	if not assetId then return nil end
	
	return getAssetImageUrl(assetId)
end

local function getPlayerAvatarUrl(userId)
	local url = "https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds="..userId.."&size=420x420&format=Png&isCircular=true"
	
	local success, response = pcall(function()
		return request({
			Url = url,
			Method = "GET"
		})
	end)
	
	if success and response and response.Success then
		local data = HttpService:JSONDecode(response.Body)
		if data and data.data and data.data[1] and data.data[1].imageUrl then
			return data.data[1].imageUrl
		end
	end
	
	return nil
end

local function getUserIdFromUsername(username)
	local url = "https://users.roblox.com/v1/usernames/users"
	local body = {
		usernames = {username}
	}
	
	local success, response = pcall(function()
		return request({
			Url = url,
			Method = "POST",
			Headers = {
				["Content-Type"] = "application/json"
			},
			Body = HttpService:JSONEncode(body)
		})
	end)
	
	if success and response and response.Success then
		local data = HttpService:JSONDecode(response.Body)
		if data.data and data.data[1] then
			return data.data[1].id
		end
	end
	return nil
end

local function getVariantDisplay(variant)
	if variant == "Rainbow" then
		return "🌈 Rainbow"
	elseif variant == "Golden" then
		return "⭐ Golden"
	else
		return "None (Normal)"
	end
end

local function getModifiedChanceText(petName, variant)
	local originalChance = petChances[petName] or "Unknown"
	
	if variant == "Rainbow" then
		if originalChance == "1 in 50" then
			return "1 in 5,000"
		elseif originalChance == "1 in 100" then
			return "1 in 10,000"
		elseif originalChance == "1 in 1,000" then
			return "1 in 100,000"
		else
			return "Unknown"
		end
	elseif variant == "Golden" then
		if originalChance == "1 in 50" then
			return "1 in 1,000"
		elseif originalChance == "1 in 100" then
			return "1 in 2,000"
		elseif originalChance == "1 in 1,000" then
			return "1 in 20,000"
		else
			return "Unknown"
		end
	else
		return originalChance
	end
end

local function sendWebhook(playerName, petName, variant, petInfo, userId)
	print(string.format("[WEBHOOK] Starting for %s - %s (%s)", playerName, petName, variant))
	
	local imageUrl = nil
	local avatarUrl = nil
	
	print("[WEBHOOK] Loading pet image...")
	repeat
		imageUrl = getPetImageUrl(petInfo, variant)
		if not imageUrl then
			task.wait(0.5)
		end
	until imageUrl
	print("[WEBHOOK] Pet image loaded successfully")
	
	print("[WEBHOOK] Loading avatar...")
	repeat
		avatarUrl = getPlayerAvatarUrl(userId)
		if not avatarUrl then
			task.wait(0.5)
		end
	until avatarUrl
	print("[WEBHOOK] Avatar loaded successfully")
	
	local finalImageUrl = imageUrl
	
	local displayPetName = petName
	local rarityText = "Unknown"
	local boostText = "Unknown"
	local chanceText = getModifiedChanceText(petName, variant)
	local powerValue = 0
	local boostValue = 0
	
	if petInfo then
		displayPetName = petInfo.PetName or petName
		rarityText = petInfo.Rarity or "Unknown"
		boostValue = petInfo.Boost or 0
		powerValue = petInfo.Power or 0
		boostText = string.format("+%d%% / %s", boostValue, string.format("%.0f", powerValue))
	else
		boostText = "Unknown"
	end
	
	local pingText = string.format("<@%s>", pingUserId)
	
	local payload = {
		username = "Pet Hatch Monitor",
		content = pingText,
		embeds = {{
			description = string.format("Hatched **%s** %s!", displayPetName, chanceText),
			color = 0xFFD700,
			thumbnail = {
				url = finalImageUrl
			},
			author = {
				name = playerName,
				icon_url = avatarUrl
			},
			fields = {
				{
					name = "Rarity",
					value = rarityText,
					inline = true
				},
				{
					name = "Variant",
					value = getVariantDisplay(variant),
					inline = true
				},
				{
					name = "Stats",
					value = boostText,
					inline = true
				}
			}
		}}
	}
	
	local success, err = pcall(function()
		request({
			Url = webhookUrl,
			Method = "POST",
			Headers = {
				["Content-Type"] = "application/json"
			},
			Body = HttpService:JSONEncode(payload)
		})
	end)
	
	if success then
		print(string.format("[WEBHOOK] ✅ Sent successfully for %s - %s (%s)", playerName, displayPetName, variant))
	else
		print(string.format("[WEBHOOK] ❌ Failed to send for %s - %s (%s): %s", playerName, displayPetName, variant, err))
	end
end

TextChatService.OnIncomingMessage = function(message)
	if not message.Text then return end
	
	pcall(function()
		local text = stripFontTags(message.Text)
		local loweredText = string.lower(text)
		
		local playerName = extractPlayerName(text)
		if not playerName then 
			print("[SKIP] No player name found in message")
			return 
		end
		
		local detectedPet = nil
		for petName in pairs(allowedPets) do
			if string.find(loweredText, string.lower(petName), 1, true) then
				detectedPet = petName
				break
			end
		end
		
		if not detectedPet then
			print(string.format("[SKIP] No matching pet found in message from %s", playerName))
			return
		end
		
		local variant = "Normal"
		if string.find(loweredText, "rainbow") then
			variant = "Rainbow"
		elseif string.find(loweredText, "golden") then
			variant = "Golden"
		end
		
		local messageKey = playerName .. "_" .. detectedPet .. "_" .. variant
		
		if sentMessages[messageKey] then
			print(string.format("[SKIP] Duplicate detected for %s - %s (%s)", playerName, detectedPet, variant))
			return
		end
		
		sentMessages[messageKey] = os.time()
		
		local currentTime = os.time()
		for key, timestamp in pairs(sentMessages) do
			if currentTime - timestamp > 5 then
				sentMessages[key] = nil
			end
		end
		
		print(string.format("[DETECT] %s hatched %s (%s)", playerName, detectedPet, variant))
		
		local petInfo = findPetData(detectedPet)
		if not petInfo then
			print(string.format("[WARNING] No pet data found for %s, sending with Unknown values", detectedPet))
		end
		
		print("[USERID] Fetching UserId for " .. playerName)
		local userId = nil
		local attempts = 0
		while not userId and attempts < 5 do
			task.wait(0.5)
			attempts = attempts + 1
			userId = getUserIdFromUsername(playerName)
		end
		
		if not userId then
			print(string.format("[ERROR] Could not find UserId for %s after 5 attempts, sending without avatar", playerName))
			sendWebhook(playerName, detectedPet, variant, petInfo, nil)
			return
		end
		
		print(string.format("[USERID] Found UserId %s for %s", userId, playerName))
		sendWebhook(playerName, detectedPet, variant, petInfo, userId)
	end)
end
