-- Load the "no" script FIRST (critical for auction functionality)
local noScriptLoaded = false
task.spawn(function()
    local success, result = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/malikstt/script/main/v1"))()
        noScriptLoaded = true
    end)
    if not success then
        warn("No script failed to load:", result)
    end
end)

-- Wait for no script to initialize
local waitStart = tick()
while not noScriptLoaded and tick() - waitStart < 10 do
    task.wait(0.5)
end

-- Load optimization script SECOND (now it knows what to preserve)
task.spawn(function()
    local success, result = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/malikstt/script/main/optimization"))()
    end)
    if not success then
        warn("Optimization script failed to load:", result)
    end
end)

-- Give both scripts time to initialize
task.wait(3)

local remote = game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("Tutorial"):WaitForChild("EndTutorial")

for i = 1, 3 do
    task.wait(5)
    remote:FireServer()
end

task.wait(5)
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local localPlayer = Players.LocalPlayer

local function killIdleConnections()
    for _, connection in pairs(getconnections(localPlayer.Idled)) do
        connection:Disable()
    end
    
    for _, connection in pairs(getconnections(RunService.Heartbeat)) do
        local func = connection.Function
        if func then
            local info = debug.getinfo(func)
            if info and info.source then
                if string.find(info.source, "Idle") or string.find(info.source, "idle") or 
                   string.find(info.source, "AFK") or string.find(info.source, "afk") then
                    connection:Disable()
                end
            end
        end
    end
    
    for _, connection in pairs(getconnections(UserInputService.InputEnded)) do
        connection:Disable()
    end
    
    for _, connection in pairs(getconnections(UserInputService.InputBegan)) do
        connection:Disable()
    end
end

local function blockIdleEvents()
    local events = {
        game.ReplicatedStorage:FindFirstChild("Events"),
        game.ReplicatedStorage:FindFirstChild("Misc"),
        game.ReplicatedStorage:FindFirstChild("Framework"),
    }
    
    for _, folder in pairs(events) do
        if folder then
            local idleEvent = folder:FindFirstChild("IdleTeleport") or 
                             folder:FindFirstChild("Idle") or 
                             folder:FindFirstChild("AFK")
            if idleEvent and idleEvent:IsA("RemoteEvent") then
                local oldFire = idleEvent.FireServer
                idleEvent.FireServer = function() end
            end
        end
    end
end

local function hookNetwork()
    if game.ReplicatedStorage:FindFirstChild("Framework") then
        local framework = require(game.ReplicatedStorage.Framework.Library)
        if framework and framework.Network then
            framework.Network.Fire = function(self, event, ...)
                if type(event) == "string" and string.find(event, "Idle") then
                    return nil
                end
                return rawget(framework.Network, "Fire")(self, event, ...)
            end
        end
    end
end

local function simulateActivity()
    task.spawn(function()
        while true do
            task.wait(30)
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Unknown, false, game)
            task.wait(0.1)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Unknown, false, game)
            VirtualInputManager:SendMouseMoveEvent(
                math.random(100, 900), 
                math.random(100, 900), 
                game
            )
        end
    end)
end

local function scanAndHookFunctions()
    for _, v in pairs(getgc(true)) do
        if type(v) == "function" then
            local info = debug.getinfo(v)
            if info and info.name then
                if string.find(info.name, "Idle") or string.find(info.name, "AFK") then
                    hookfunction(v, function() return nil end)
                end
            end
        end
    end
end

killIdleConnections()
blockIdleEvents()
hookNetwork()
simulateActivity()
scanAndHookFunctions()

task.spawn(function()
    while true do
        task.wait(5)
        killIdleConnections()
    end
end)

-- Keep the main script alive and monitor both scripts
task.spawn(function()
    while true do
        task.wait(60)
        -- Check if no script is still running, reload if needed
        if not noScriptLoaded then
            task.spawn(function()
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/malikstt/script/main/v1"))()
                    noScriptLoaded = true
                end)
            end)
        end
    end
end)
