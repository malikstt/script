local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")
local VirtualInput = game:GetService("VirtualInputManager")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local Paper = require(ReplicatedStorage:WaitForChild("Paper"))
local Stats = Paper.Stats
local Network = Paper.Network

repeat task.wait() until Stats.IsLoaded(LocalPlayer)

local Strength = Stats.Get("Strength")
local AutoThrow = Stats.Get("AutoThrow")
local AutoTrain = Stats.Get("AutoTrain")
local mouse = LocalPlayer:GetMouse()

-- Strength tracking
local strengthHistory = {}
local lastStrength = 0
local lastUpdateTime = os.clock()
local targetStrength = 1e15 -- 1 Qa

local function FormatNumber(num)
    if num >= 1e33 then
        return string.format("%.2fDc", num / 1e33)
    elseif num >= 1e30 then
        return string.format("%.2fN", num / 1e30)
    elseif num >= 1e27 then
        return string.format("%.2fOc", num / 1e27)
    elseif num >= 1e24 then
        return string.format("%.2fSp", num / 1e24)
    elseif num >= 1e21 then
        return string.format("%.2fSx", num / 1e21)
    elseif num >= 1e18 then
        return string.format("%.2fQi", num / 1e18)
    elseif num >= 1e15 then
        return string.format("%.2fQa", num / 1e15)
    elseif num >= 1e12 then
        return string.format("%.2fT", num / 1e12)
    elseif num >= 1e9 then
        return string.format("%.2fB", num / 1e9)
    elseif num >= 1e6 then
        return string.format("%.2fM", num / 1e6)
    elseif num >= 1e3 then
        return string.format("%.2fK", num / 1e3)
    else
        return tostring(math.floor(num))
    end
end

local function FormatTime(seconds)
    if seconds < 0 then return "0s" end
    if seconds < 60 then
        return string.format("%.0fs", seconds)
    elseif seconds < 3600 then
        return string.format("%.0fm", seconds / 60)
    elseif seconds < 86400 then
        return string.format("%.0fh", seconds / 3600)
    else
        return string.format("%.0fd", seconds / 86400)
    end
end

local function GetStrengthPerMinute()
    local currentStrength = Strength and Strength.Value or 0
    local currentTime = os.clock()
    local timeDiff = currentTime - lastUpdateTime
    
    if timeDiff >= 60 and lastStrength > 0 then
        local gain = currentStrength - lastStrength
        lastStrength = currentStrength
        lastUpdateTime = currentTime
        return gain
    elseif timeDiff >= 60 then
        lastStrength = currentStrength
        lastUpdateTime = currentTime
        return 0
    end
    
    return 0
end

local function GetETA()
    local currentStrength = Strength and Strength.Value or 0
    if currentStrength >= targetStrength then
        return 0
    end
    
    local gainPerMinute = GetStrengthPerMinute()
    if gainPerMinute <= 0 then
        return -1
    end
    
    local needed = targetStrength - currentStrength
    local minutesNeeded = needed / gainPerMinute
    return minutesNeeded * 60
end

local function GetUIColor(strength)
    if strength >= 1e33 then
        local hue = (tick() % 5) / 5
        return Color3.fromHSV(hue, 1, 0.7)
    elseif strength >= 1e30 then
        return Color3.fromRGB(200, 100, 255)
    elseif strength >= 1e27 then
        return Color3.fromRGB(100, 200, 255)
    elseif strength >= 1e24 then
        return Color3.fromRGB(255, 100, 200)
    elseif strength >= 1e21 then
        return Color3.fromRGB(160, 80, 240)
    elseif strength >= 1e18 then
        return Color3.fromRGB(255, 50, 50)
    elseif strength >= 1e15 then
        return Color3.fromRGB(255, 100, 100)
    elseif strength >= 1e12 then
        return Color3.fromRGB(255, 150, 50)
    elseif strength >= 1e11 then
        return Color3.fromRGB(255, 255, 80)
    elseif strength >= 1e10 then
        return Color3.fromRGB(80, 255, 80)
    else
        return Color3.fromRGB(20, 20, 25)
    end
end

local function GetScale()
    local viewport = workspace.CurrentCamera.ViewportSize
    return math.min(viewport.X / 1920, viewport.Y / 1080)
end

local playerGui = LocalPlayer:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TrainUI"
screenGui.ResetOnSpawn = false
screenGui.IgnoreGuiInset = true
screenGui.Parent = playerGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(1, 0, 1, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

local centerContainer = Instance.new("Frame")
centerContainer.Size = UDim2.new(0, 550, 0, 500)
centerContainer.Position = UDim2.new(0.5, -275, 0.5, -250)
centerContainer.BackgroundTransparency = 1
centerContainer.Parent = mainFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 50)
titleLabel.BackgroundTransparency = 1
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 38
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Text = "⚔️ TRAINING MODE"
titleLabel.TextXAlignment = Enum.TextXAlignment.Center
titleLabel.Parent = centerContainer

local strengthLabel = Instance.new("TextLabel")
strengthLabel.Size = UDim2.new(1, 0, 0, 80)
strengthLabel.Position = UDim2.new(0, 0, 0, 55)
strengthLabel.BackgroundTransparency = 1
strengthLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
strengthLabel.TextSize = 64
strengthLabel.Font = Enum.Font.GothamBold
strengthLabel.Text = "0"
strengthLabel.TextXAlignment = Enum.TextXAlignment.Center
strengthLabel.Parent = centerContainer

local perMinuteLabel = Instance.new("TextLabel")
perMinuteLabel.Size = UDim2.new(1, 0, 0, 30)
perMinuteLabel.Position = UDim2.new(0, 0, 0, 140)
perMinuteLabel.BackgroundTransparency = 1
perMinuteLabel.TextColor3 = Color3.fromRGB(200, 200, 100)
perMinuteLabel.TextSize = 16
perMinuteLabel.Font = Enum.Font.GothamBold
perMinuteLabel.Text = "⚡ 0/min"
perMinuteLabel.TextXAlignment = Enum.TextXAlignment.Center
perMinuteLabel.Parent = centerContainer

local etaLabel = Instance.new("TextLabel")
etaLabel.Size = UDim2.new(1, 0, 0, 30)
etaLabel.Position = UDim2.new(0, 0, 0, 175)
etaLabel.BackgroundTransparency = 1
etaLabel.TextColor3 = Color3.fromRGB(100, 200, 255)
etaLabel.TextSize = 16
etaLabel.Font = Enum.Font.GothamBold
etaLabel.Text = "⏱️ ETA to 1Qa: Calculating..."
etaLabel.TextXAlignment = Enum.TextXAlignment.Center
etaLabel.Parent = centerContainer

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, 0, 0, 35)
statusLabel.Position = UDim2.new(0, 0, 0, 215)
statusLabel.BackgroundTransparency = 1
statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
statusLabel.TextSize = 18
statusLabel.Font = Enum.Font.GothamBold
statusLabel.Text = "AUTO-TRAIN: ENABLED"
statusLabel.TextXAlignment = Enum.TextXAlignment.Center
statusLabel.Parent = centerContainer

local subLabel = Instance.new("TextLabel")
subLabel.Size = UDim2.new(1, 0, 0, 25)
subLabel.Position = UDim2.new(0, 0, 0, 255)
subLabel.BackgroundTransparency = 1
subLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
subLabel.TextSize = 12
subLabel.Font = Enum.Font.Gotham
subLabel.Text = "Auto-Throw disabled | Training only"
subLabel.TextXAlignment = Enum.TextXAlignment.Center
subLabel.Parent = centerContainer

local progressBg = Instance.new("Frame")
progressBg.Size = UDim2.new(1, -40, 0, 20)
progressBg.Position = UDim2.new(0, 20, 0, 295)
progressBg.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
progressBg.BorderSizePixel = 0
progressBg.Parent = centerContainer

local progressCorner = Instance.new("UICorner")
progressCorner.CornerRadius = UDim.new(0, 10)
progressCorner.Parent = progressBg

local progressFill = Instance.new("Frame")
progressFill.Size = UDim2.new(0, 0, 1, 0)
progressFill.BackgroundColor3 = Color3.fromRGB(100, 255, 100)
progressFill.BorderSizePixel = 0
progressFill.Parent = progressBg

local fillCorner = Instance.new("UICorner")
fillCorner.CornerRadius = UDim.new(0, 10)
fillCorner.Parent = progressFill

local thresholdLabels = Instance.new("Frame")
thresholdLabels.Size = UDim2.new(1, 0, 0, 20)
thresholdLabels.Position = UDim2.new(0, 0, 0, 320)
thresholdLabels.BackgroundTransparency = 1
thresholdLabels.Parent = centerContainer

local thresholds = {"1B", "10B", "100B", "1T", "1Qa"}
local positions = {0.2, 0.4, 0.6, 0.8, 1}

for i, text in ipairs(thresholds) do
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0, 60, 1, 0)
    label.Position = UDim2.new(positions[i], -30, 0, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(120, 120, 140)
    label.TextSize = 11
    label.Font = Enum.Font.Gotham
    label.Text = text
    label.TextXAlignment = Enum.TextXAlignment.Center
    label.Parent = thresholdLabels
end

local function OnScreenResize()
    local scale = GetScale()
    titleLabel.TextSize = 38 * scale
    strengthLabel.TextSize = 64 * scale
    perMinuteLabel.TextSize = 16 * scale
    etaLabel.TextSize = 16 * scale
    statusLabel.TextSize = 18 * scale
    subLabel.TextSize = 12 * scale
    centerContainer.Size = UDim2.new(0, 550 * scale, 0, 500 * scale)
    centerContainer.Position = UDim2.new(0.5, -275 * scale, 0.5, -250 * scale)
end

workspace.CurrentCamera:GetPropertyChangedSignal("ViewportSize"):Connect(OnScreenResize)
OnScreenResize()

local function UpdateProgressBar(strength)
    local percent = math.min(strength / targetStrength, 1)
    local width = percent * (progressBg.AbsoluteSize.X - 4)
    progressFill:TweenSize(UDim2.new(0, width, 1, 0), "Out", "Quad", 0.3, true)
    
    if strength >= 1e15 then
        progressFill.BackgroundColor3 = Color3.fromRGB(255, 100, 100)
    elseif strength >= 1e12 then
        progressFill.BackgroundColor3 = Color3.fromRGB(255, 150, 50)
    elseif strength >= 1e11 then
        progressFill.BackgroundColor3 = Color3.fromRGB(255, 255, 80)
    elseif strength >= 1e10 then
        progressFill.BackgroundColor3 = Color3.fromRGB(80, 255, 80)
    else
        progressFill.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
    end
end

local lastColor = nil
local lastPerMinuteUpdate = 0

local function UpdateUI()
    pcall(function()
        if not Strength then return end
        local strength = Strength.Value or 0
        local color = GetUIColor(strength)
        
        if color ~= lastColor then
            TweenService:Create(mainFrame, TweenInfo.new(0.5), {BackgroundColor3 = color}):Play()
            lastColor = color
        end
        
        strengthLabel.Text = FormatNumber(strength)
        UpdateProgressBar(strength)
        
        -- Update per-minute and ETA every 60 seconds
        local currentTime = os.clock()
        if currentTime - lastPerMinuteUpdate >= 60 then
            local perMinute = GetStrengthPerMinute()
            perMinuteLabel.Text = string.format("⚡ %s/min", FormatNumber(perMinute))
            
            local eta = GetETA()
            if eta > 0 then
                etaLabel.Text = string.format("⏱️ ETA to 1Qa: %s", FormatTime(eta))
            elseif eta == 0 then
                etaLabel.Text = "✅ 1Qa REACHED!"
            else
                etaLabel.Text = "⏱️ ETA to 1Qa: Calculating..."
            end
            lastPerMinuteUpdate = currentTime
        end
        
        if AutoThrow and AutoThrow.Value then
            Network.FireServer("Toggle Setting", "AutoThrow")
            statusLabel.Text = "⚠️ AUTO-THROW: DISABLED"
            statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
            task.wait(0.3)
            statusLabel.Text = "✅ AUTO-TRAIN: ENABLED"
            statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
        end
        
        if AutoTrain and not AutoTrain.Value then
            Network.FireServer("Toggle Setting", "AutoTrain")
            statusLabel.Text = "✅ AUTO-TRAIN: ENABLED"
            statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
        end
    end)
end

pcall(function()
    local idledConnections = getconnections(LocalPlayer.Idled)
    for _, connection in pairs(idledConnections) do
        pcall(function() connection:Disable() end)
    end
end)

LocalPlayer.Idled:Connect(function()
    pcall(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new(0,0))
    end)
    task.wait(0.1)
    pcall(function()
        VirtualInput:SendMouseMotionEvent(1, 1, mouse.X, mouse.Y, game)
        task.wait(0.05)
        VirtualInput:SendMouseMotionEvent(-1, -1, mouse.X, mouse.Y, game)
    end)
end)

task.spawn(function()
    while true do
        task.wait(45)
        pcall(function()
            VirtualInput:SendMouseMotionEvent(math.random(-2, 2), math.random(-2, 2), mouse.X, mouse.Y, game)
        end)
    end
end)

-- Heartbeat update
RunService.Heartbeat:Connect(function()
    pcall(UpdateUI)
end)

task.wait(3)
print("[Train Mode] Loaded - Heartbeat updates enabled")
print("[Train Mode] Target: 1 Qa (1,000,000,000,000,000)")
