local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

pcall(function()
	settings().Rendering.QualityLevel = 1
end)
pcall(function()
	workspace:SetAttribute("3dRenderingEnabled", false)
end)

local DataClient = require(
	ReplicatedStorage:WaitForChild("Packages"):WaitForChild("DataService")
).client

local data = DataClient._data._data

local LocalPlayer = Players.LocalPlayer

local UPDATE_INTERVAL = 0.5
local STATUS_INTERVAL = 5
local TWEEN_TIME = 0.25

local PALETTE = {
	bg      = Color3.fromRGB(5, 5, 8),
	panel   = Color3.fromRGB(10, 12, 18),
	border  = Color3.fromRGB(30, 35, 50),
	accent  = Color3.fromRGB(80, 160, 255),
	gold    = Color3.fromRGB(255, 210, 60),
	diamond = Color3.fromRGB(120, 230, 255),
	void    = Color3.fromRGB(180, 80, 255),
	text    = Color3.fromRGB(220, 228, 245),
	dim     = Color3.fromRGB(100, 110, 135),
}

local ASSET_IDS = {
	rebirth = "rbxassetid://132563621658654",
	goop    = "rbxassetid://114367663524453",
	coins   = "rbxassetid://103275559073812",
	kills   = "rbxassetid://111148706433634",
	zone    = "rbxassetid://96591575070730",
	goldDice = "rbxassetid://133158717152423",
	diamondDice = "rbxassetid://139581611167886",
	voidDice = "rbxassetid://98779770728903",
}

local DICE_FRAMES = {"|", "/", "-", "\\"}

local SUFFIXES = {
	{1e15, "Qd"},
	{1e12, "T"},
	{1e9,  "B"},
	{1e6,  "M"},
	{1e3,  "K"},
}

local function formatNum(n)
	n = tonumber(n) or 0
	for _, pair in ipairs(SUFFIXES) do
		local threshold, suffix = pair[1], pair[2]
		if n >= threshold then
			local v = n / threshold
			local s = string.format("%.2f", v)
			s = s:gsub("%.?0+$", "")
			return s .. suffix
		end
	end
	return tostring(math.floor(n))
end

local function tweenProp(obj, props, t)
	t = t or TWEEN_TIME
	TweenService:Create(obj, TweenInfo.new(t, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), props):Play()
end

local function pulse(label)
	tweenProp(label, {TextTransparency = 0.6}, 0.05)
	task.delay(0.05, function()
		tweenProp(label, {TextTransparency = 0}, 0.2)
	end)
end

local function make(class, props, parent)
	local obj = Instance.new(class)
	for k, v in pairs(props) do
		obj[k] = v
	end
	if parent then obj.Parent = parent end
	return obj
end

local function makeFrame(props, parent)
	props.BackgroundTransparency = props.BackgroundTransparency or 0
	return make("Frame", props, parent)
end

local function makeLabel(props, parent)
	props.BackgroundTransparency = 1
	props.Font = props.Font or Enum.Font.GothamBold
	props.TextScaled = true
	props.TextWrapped = false
	return make("TextLabel", props, parent)
end

local function makeImageLabel(props, parent)
	props.BackgroundTransparency = 1
	props.ScaleType = props.ScaleType or Enum.ScaleType.Fit
	return make("ImageLabel", props, parent)
end

local function addCorner(parent, radius)
	make("UICorner", {CornerRadius = UDim.new(0, radius or 6)}, parent)
end

local function addStroke(parent, color, thickness)
	make("UIStroke", {
		Color = color or PALETTE.border,
		Thickness = thickness or 1,
		ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
	}, parent)
end

local function addListLayout(parent, props)
	local ul = Instance.new("UIListLayout")
	ul.FillDirection = props.FillDirection or Enum.FillDirection.Vertical
	ul.HorizontalAlignment = props.HAlign or Enum.HorizontalAlignment.Center
	ul.VerticalAlignment = props.VAlign or Enum.VerticalAlignment.Top
	ul.Padding = props.Padding or UDim.new(0, 12)
	ul.SortOrder = Enum.SortOrder.LayoutOrder
	ul.Parent = parent
	return ul
end

local function getPlayerThumbnail(userId)
	local ok, img = pcall(function()
		return Players:GetUserThumbnailAsync(userId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
	end)
	if ok then return img end
	return ""
end

local function buildHUD(playerGui)
	local existing = playerGui:FindFirstChild("__MAINHUD__")
	if existing then existing:Destroy() end

	local screenGui = make("ScreenGui", {
		Name = "__MAINHUD__",
		ResetOnSpawn = false,
		IgnoreGuiInset = true,
		DisplayOrder = 9999,
		ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
		Parent = playerGui,
	})

	local scaleContainer = makeFrame({
		Name = "ScaleContainer",
		Size = UDim2.fromScale(1, 1),
		Position = UDim2.fromScale(0, 0),
		BackgroundColor3 = PALETTE.bg,
		BackgroundTransparency = 0,
	}, screenGui)

	make("UIScale", {Scale = 1}, scaleContainer)

	make("UIAspectRatioConstraint", {
		AspectRatio = 16 / 9,
		AspectType = Enum.AspectType.ScaleWithParentSize,
		DominantAxis = Enum.DominantAxis.Width,
	}, scaleContainer)

	makeFrame({
		Name = "Overlay",
		Size = UDim2.fromScale(1, 1),
		BackgroundColor3 = Color3.new(0, 0, 0),
		BackgroundTransparency = 0.96,
		ZIndex = 10,
	}, scaleContainer)

	local topCenter = makeFrame({
		Name = "TopCenter",
		Size = UDim2.fromScale(0.28, 0.09),
		Position = UDim2.fromScale(0.36, 0.01),
		BackgroundColor3 = PALETTE.panel,
	}, scaleContainer)
	addCorner(topCenter, 8)
	addStroke(topCenter, PALETTE.accent, 1)

	local avatarFrame = makeFrame({
		Name = "AvatarFrame",
		Size = UDim2.fromScale(0.18, 0.85),
		Position = UDim2.fromScale(0.02, 0.075),
		BackgroundColor3 = PALETTE.border,
	}, topCenter)
	addCorner(avatarFrame, 6)
	make("UIAspectRatioConstraint", {
		AspectRatio = 1,
		AspectType = Enum.AspectType.ScaleWithParentSize,
		DominantAxis = Enum.DominantAxis.Height,
	}, avatarFrame)

	local avatarImg = makeImageLabel({
		Name = "Avatar",
		Size = UDim2.fromScale(1, 1),
		Position = UDim2.fromScale(0, 0),
		Image = "",
		ScaleType = Enum.ScaleType.Fit,
	}, avatarFrame)
	addCorner(avatarImg, 6)

	local usernameLabel = makeLabel({
		Name = "Username",
		Size = UDim2.fromScale(0.77, 1),
		Position = UDim2.fromScale(0.21, 0),
		Text = LocalPlayer.Name,
		TextColor3 = PALETTE.text,
		Font = Enum.Font.GothamBold,
		TextXAlignment = Enum.TextXAlignment.Center,
	}, topCenter)

	task.spawn(function()
		local img = getPlayerThumbnail(LocalPlayer.UserId)
		avatarImg.Image = img
	end)

	local timerContainer = makeFrame({
		Name = "TimerContainer",
		Size = UDim2.fromScale(0.18, 0.1),
		Position = UDim2.fromScale(0.41, 0.45),
		BackgroundColor3 = PALETTE.panel,
	}, scaleContainer)
	addCorner(timerContainer, 12)
	addStroke(timerContainer, PALETTE.accent, 2)

	local timerLabel = makeLabel({
		Name = "Timer",
		Size = UDim2.fromScale(1, 1),
		Text = "00:00",
		TextColor3 = PALETTE.accent,
		Font = Enum.Font.GothamBold,
		TextScaled = true,
	}, timerContainer)

	local leftPanel = makeFrame({
		Name = "LeftPanel",
		Size = UDim2.fromScale(0.18, 0.78),
		Position = UDim2.fromScale(0.01, 0.11),
		BackgroundColor3 = PALETTE.panel,
	}, scaleContainer)
	addCorner(leftPanel, 10)
	addStroke(leftPanel, PALETTE.border)

	local leftPadding = make("UIPadding", {
		PaddingTop = UDim.new(0.02, 0),
		PaddingBottom = UDim.new(0.02, 0),
		PaddingLeft = UDim.new(0.04, 0),
		PaddingRight = UDim.new(0.04, 0),
	}, leftPanel)

	local leftLayout = addListLayout(leftPanel, {
		Padding = UDim.new(0, 12),
	})

	local function makeStatRow(parent, assetId, startVal)
		local row = makeFrame({
			Size = UDim2.fromScale(1, 0.18),
			BackgroundColor3 = Color3.fromRGB(14, 16, 24),
		}, parent)
		addCorner(row, 6)

		local iconContainer = makeFrame({
			Name = "IconContainer",
			Size = UDim2.fromScale(0.22, 1),
			BackgroundTransparency = 1,
		}, row)

		local iconImg = makeImageLabel({
			Size = UDim2.fromScale(1, 0.7),
			Position = UDim2.fromScale(0, 0.15),
			Image = assetId,
			ScaleType = Enum.ScaleType.Fit,
		}, iconContainer)
		make("UIAspectRatioConstraint", {
			AspectRatio = 1,
			AspectType = Enum.AspectType.ScaleWithParentSize,
			DominantAxis = Enum.DominantAxis.Height,
		}, iconImg)

		local valLbl = makeLabel({
			Size = UDim2.fromScale(0.74, 0.8),
			Position = UDim2.fromScale(0.24, 0.1),
			Text = startVal or "0",
			TextColor3 = PALETTE.text,
			Font = Enum.Font.GothamBold,
			TextXAlignment = Enum.TextXAlignment.Right,
		}, row)

		return row, valLbl
	end

	local function makeStatRowWithEmoji(parent, emoji, startVal)
		local row = makeFrame({
			Size = UDim2.fromScale(1, 0.18),
			BackgroundColor3 = Color3.fromRGB(14, 16, 24),
		}, parent)
		addCorner(row, 6)

		local iconContainer = makeFrame({
			Name = "IconContainer",
			Size = UDim2.fromScale(0.22, 1),
			BackgroundTransparency = 1,
		}, row)

		local emojiLabel = makeLabel({
			Size = UDim2.fromScale(1, 0.7),
			Position = UDim2.fromScale(0, 0.15),
			Text = emoji,
			TextColor3 = PALETTE.accent,
			Font = Enum.Font.GothamBold,
			TextScaled = true,
		}, iconContainer)

		local valLbl = makeLabel({
			Size = UDim2.fromScale(0.74, 0.8),
			Position = UDim2.fromScale(0.24, 0.1),
			Text = startVal or "0",
			TextColor3 = PALETTE.text,
			Font = Enum.Font.GothamBold,
			TextXAlignment = Enum.TextXAlignment.Right,
		}, row)

		return row, valLbl
	end

	local function makeProgressionRow(parent, assetId, labelText, accentColor, startVal)
		local row = makeFrame({
			Size = UDim2.fromScale(1, 0.18),
			BackgroundColor3 = Color3.fromRGB(14, 16, 24),
		}, parent)
		addCorner(row, 6)

		local iconContainer = makeFrame({
			Name = "IconContainer",
			Size = UDim2.fromScale(0.22, 1),
			BackgroundTransparency = 1,
		}, row)

		local iconImg = makeImageLabel({
			Size = UDim2.fromScale(1, 0.7),
			Position = UDim2.fromScale(0, 0.15),
			Image = assetId,
			ScaleType = Enum.ScaleType.Fit,
		}, iconContainer)
		make("UIAspectRatioConstraint", {
			AspectRatio = 1,
			AspectType = Enum.AspectType.ScaleWithParentSize,
			DominantAxis = Enum.DominantAxis.Height,
		}, iconImg)

		local textLabel = makeLabel({
			Size = UDim2.fromScale(0.52, 0.8),
			Position = UDim2.fromScale(0.24, 0.1),
			Text = labelText,
			TextColor3 = PALETTE.dim,
			Font = Enum.Font.Gotham,
			TextXAlignment = Enum.TextXAlignment.Left,
			TextScaled = true,
		}, row)

		local valLbl = makeLabel({
			Size = UDim2.fromScale(0.22, 0.8),
			Position = UDim2.fromScale(0.76, 0.1),
			Text = startVal or "-",
			TextColor3 = accentColor,
			Font = Enum.Font.GothamBold,
			TextXAlignment = Enum.TextXAlignment.Right,
		}, row)

		return row, valLbl
	end

	local rebirthRow, rebirthVal = makeStatRow(leftPanel, ASSET_IDS.rebirth, "0")
	local goopRow, goopVal = makeStatRow(leftPanel, ASSET_IDS.goop, "0")
	local coinsRow, coinsVal = makeStatRow(leftPanel, ASSET_IDS.coins, "0")
	
	local goldenRow, goldenVal = makeProgressionRow(leftPanel, ASSET_IDS.goldDice, "Next Golden", PALETTE.gold, "-")
	local diamondRow, diamondVal = makeProgressionRow(leftPanel, ASSET_IDS.diamondDice, "Next Diamond", PALETTE.diamond, "-")
	local voidRow, voidVal = makeProgressionRow(leftPanel, ASSET_IDS.voidDice, "Next Void", PALETTE.void, "-")

	local rightPanel = makeFrame({
		Name = "RightPanel",
		Size = UDim2.fromScale(0.18, 0.78),
		Position = UDim2.fromScale(0.81, 0.11),
		BackgroundColor3 = PALETTE.panel,
	}, scaleContainer)
	addCorner(rightPanel, 10)
	addStroke(rightPanel, PALETTE.border)

	local rightPadding = make("UIPadding", {
		PaddingTop = UDim.new(0.02, 0),
		PaddingBottom = UDim.new(0.02, 0),
		PaddingLeft = UDim.new(0.04, 0),
		PaddingRight = UDim.new(0.04, 0),
	}, rightPanel)

	local rightLayout = addListLayout(rightPanel, {
		Padding = UDim.new(0, 12),
	})

	local rollsRow, rollsVal = makeStatRowWithEmoji(rightPanel, "🎲", "0")
	local killsRow, killsVal = makeStatRow(rightPanel, ASSET_IDS.kills, "0")
	local zoneRow, zoneVal = makeStatRow(rightPanel, ASSET_IDS.zone, "0")

	local statusPanel = makeFrame({
		Name = "StatusPanel",
		Size = UDim2.fromScale(0.28, 0.06),
		Position = UDim2.fromScale(0.36, 0.7),
		BackgroundColor3 = PALETTE.panel,
	}, scaleContainer)
	addCorner(statusPanel, 20)
	addStroke(statusPanel, PALETTE.border)

	local statusLabel = makeLabel({
		Name = "StatusLabel",
		Size = UDim2.fromScale(1, 1),
		Text = "* Idle",
		TextColor3 = PALETTE.accent,
		Font = Enum.Font.GothamBold,
	}, statusPanel)

	for _, anchor in ipairs({UDim2.fromScale(0.01, 0.15), UDim2.fromScale(0.96, 0.15)}) do
		makeFrame({
			Size = UDim2.fromScale(0.012, 0.5),
			Position = anchor,
			BackgroundColor3 = PALETTE.accent,
		}, statusPanel)
	end

	return {
		screenGui = screenGui,
		timerLabel = timerLabel,
		rebirthVal = rebirthVal,
		goopVal = goopVal,
		coinsVal = coinsVal,
		rollsVal = rollsVal,
		killsVal = killsVal,
		zoneVal = zoneVal,
		goldenVal = goldenVal,
		diamondVal = diamondVal,
		voidVal = voidVal,
		statusLabel = statusLabel,
	}
end

local sessionStart = os.clock()

local function getTimerText()
	local elapsed = os.clock() - sessionStart
	local minutes = math.floor(elapsed / 60)
	local seconds = math.floor(elapsed % 60)
	return string.format("%02d:%02d", minutes, seconds)
end

local function safeGet(...)
	local current = data
	for _, key in ipairs({...}) do
		if type(current) ~= "table" then return 0 end
		current = current[key]
		if current == nil then return 0 end
	end
	return tonumber(current) or 0
end

local function runStatusSystem(refs)
	local lastStatus = ""
	local prevCoins = safeGet("coins")
	local prevRebirths = safeGet("rebirths")
	local prevGoop = safeGet("goop")
	local prevKills = safeGet("stats", "kills")
	local prevRolls = safeGet("stats", "rolls")
	local diceThread = nil

	local function setStatus(text, color)
		if text == lastStatus then return end
		lastStatus = text

		if diceThread then
			task.cancel(diceThread)
			diceThread = nil
		end

		local lbl = refs.statusLabel
		tweenProp(lbl, {TextTransparency = 1}, 0.15)
		task.delay(0.15, function()
			lbl.Text = "* " .. text
			lbl.TextColor3 = color or PALETTE.accent
			tweenProp(lbl, {TextTransparency = 0}, 0.2)
		end)
	end

	local function startDiceAnim()
		diceThread = task.spawn(function()
			local i = 1
			while true do
				refs.statusLabel.Text = DICE_FRAMES[i] .. " Rolling"
				i = (i % #DICE_FRAMES) + 1
				task.wait(0.1)
			end
		end)
	end

	local function pickStatus()
		local coins = safeGet("coins")
		local rebirths = safeGet("rebirths")
		local goop = safeGet("goop")
		local kills = safeGet("stats", "kills")
		local rolls = safeGet("stats", "rolls")

		local candidates = {}

		if coins > prevCoins then
			table.insert(candidates, {"Farming Coins", PALETTE.gold})
		end
		if rebirths > prevRebirths then
			table.insert(candidates, {
				string.format("Rebirthed %s -> %s", formatNum(prevRebirths), formatNum(rebirths)),
				PALETTE.diamond
			})
		end
		if goop > prevGoop then
			table.insert(candidates, {
				"Farming Goop: " .. formatNum(goop),
				Color3.fromRGB(80, 220, 80)
			})
		end
		if kills > prevKills then
			table.insert(candidates, {"Fighting Slimes", Color3.fromRGB(255, 80, 80)})
		end

		prevCoins = coins
		prevRebirths = rebirths
		prevGoop = goop
		prevKills = kills

		if rolls > prevRolls then
			prevRolls = rolls
			if lastStatus ~= "Rolling" then
				lastStatus = "Rolling"
				if diceThread then task.cancel(diceThread) end
				refs.statusLabel.TextColor3 = PALETTE.void
				tweenProp(refs.statusLabel, {TextTransparency = 1}, 0.15)
				task.delay(0.15, function()
					tweenProp(refs.statusLabel, {TextTransparency = 0}, 0.2)
					startDiceAnim()
				end)
			end
			return
		end

		local filtered = {}
		for _, c in ipairs(candidates) do
			if c[1] ~= lastStatus then
				table.insert(filtered, c)
			end
		end

		if #filtered > 0 then
			local choice = filtered[math.random(1, #filtered)]
			setStatus(choice[1], choice[2])
		else
			setStatus("Idle", PALETTE.dim)
		end
	end

	task.spawn(function()
		while true do
			pcall(pickStatus)
			task.wait(STATUS_INTERVAL)
		end
	end)
end

local function updateStats(refs)
	local function updateLabel(lbl, newText)
		if lbl.Text ~= newText then
			lbl.Text = newText
			pulse(lbl)
		end
	end

	updateLabel(refs.rebirthVal, formatNum(safeGet("rebirths")))
	updateLabel(refs.goopVal, formatNum(safeGet("goop")))
	updateLabel(refs.coinsVal, formatNum(safeGet("coins")))
	updateLabel(refs.rollsVal, formatNum(safeGet("stats", "rolls")))
	updateLabel(refs.killsVal, formatNum(safeGet("stats", "kills")))
	updateLabel(refs.zoneVal, formatNum(safeGet("zone")))

	local function getProgressionText(value)
		local n = tonumber(value)
		if n == nil then return "-" end
		if n == 0 then return "Ready" end
		return formatNum(n) .. " rolls"
	end

	updateLabel(refs.goldenVal, getProgressionText(safeGet("specialRollProgression", "golden", "rollsUntilNext")))
	updateLabel(refs.diamondVal, getProgressionText(safeGet("specialRollProgression", "diamond", "rollsUntilNext")))
	updateLabel(refs.voidVal, getProgressionText(safeGet("specialRollProgression", "void", "rollsUntilNext")))

	refs.timerLabel.Text = getTimerText()
end

local currentRefs = nil

local function guardAndInit(playerGui)
	currentRefs = buildHUD(playerGui)
	runStatusSystem(currentRefs)

	currentRefs.screenGui.AncestryChanged:Connect(function(_, parent)
		if parent == nil then
			task.defer(function()
				guardAndInit(playerGui)
			end)
		end
	end)
end

local playerGui = LocalPlayer:WaitForChild("PlayerGui")
guardAndInit(playerGui)

local elapsed = 0
RunService.Heartbeat:Connect(function(dt)
	elapsed += dt
	if elapsed >= UPDATE_INTERVAL then
		elapsed = 0
		if currentRefs then
			pcall(updateStats, currentRefs)
		end
	end
end)
